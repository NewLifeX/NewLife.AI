# NewLife.AI 功能审计（2026-09，第 16 轮全库重审）

> 版本：v1.0 | 日期：2026-09-05
> 范围：`NAI/NewLife.AI/` 全库 12 维度重审，双主线深审（协议客户端 / 模型特征识别），含 08-17 后全部新增/重构面。
> 背景：历史两份文档（`基础库审计.md` 停 2026-08-17 第 15 轮续六 / `协议客户端分析.md` 停 v4.8 十二轮）基线全量 1632。此后 NAI 新增大量未审变更（模型元数据体系 / MCP 自包含重构 / 工具循环治理 / TokenEstimator / Compaction 等）。本轮审计确认旧修复在位 + 新面全覆盖后，两份旧文档删除。

---

## 1. 基线（Phase 0）

- 构建：`NAI/NewLife.AI.sln` 0 错误；NewLife.AI 库代码 0 编译警告（2 个 SourceLink 环境警告非代码，submodule 无 git 源链接）
- XUnitTest 项目警告：445（含 CS8632/McpxUnit 等，测试项目基线，非库代码）
- 全量测试：**1936 通过 / 0 失败 / 7 跳过**（总计 1943，1m31s）
- 遗留核对：AssemblyVersion=1.6.* 通配符（CS8357，仅 BenchmarkDotNet 样板工程 Deterministic 触发）——沿用第 14 轮遗留，不动（需确认 NewLife 版本体系约定）

---

## 2. 问题登记表

> 严重级：**P0** 崩溃/计费错误/安全 | **P1** 功能缺陷/一致性 | **P2** 代码卫生/健壮性/性能 | **P3** 低风险
> 状态：🆕 已登记 | 🔧 修复中 | ✅ 已修复 | ⏸ 暂缓 | ❌ 误报关闭

| 编号 | 严重级 | 维度 | 问题 | 修复方案 | 状态 |
|:----:|:------:|:----:|------|----------|:----:|
| B-01 | **P1** | 模型识别 | **claude/gemini 家族缺失**：ModelFamilies 17 家族无 claude/gemini（ModelFamilyTests 仍断言 `Match("claude-sonnet-4")==null` 固化缺陷）；Anthropic/Gemini/Bedrock 客户端（继承 AiClientBase）无家族推断，claude-*/gemini-* 新版本能力位全 false | 新增 claude/gemini 家族（数据源 ModelData anthropic/gemini.json）；同步更新测试断言 | ✅ |
| B-02 | **P1** | 模型识别 | glm 家族规则不准：glm-4.5v/4.6v（及 -flash 变体）被推断 thinking=true + 262K 上下文（实际不思考、131K/64K，高估超窗风险）；glm-4.7-flash 思考误判（glm-4-flash* 模式不匹配 4.7 点号版本）；glm-5.x 上下文 262K vs 实际 1M（低估） | 对照 ModelData/zhipu.json + dashscope 托管修正规则（-v 视觉版不思考分档、flash 点号版、5.x=1M） | ✅ |
| B-03 | P2 | 模型识别 | kimi 家族：kimi-k3 上下文 262K vs 实际 1M（低估）；kimi-k2.6/k2.7-code vision 漏标（家族 kimi-k2* vision:false，实际 k2.6/k2.7-code 支持视觉） | 对照 ModelData/moonshot.json 修正 | ✅ |
| B-04 | P2 | 模型识别 | MiniMax 家族：MiniMax-M3 上下文 196608 vs 实际 1M（低估） | 对照 ModelData/minimax.json 修正 | ✅ |
| B-05 | **P1** | 模型识别 | ernie 家族：主档无 context 规则（DefaultContext 8192 → ernie-5.1 推断仅 8K vs 实际 131K，严重低估不必要限制）；ernie-5* vision 误判 true（实际 5.x 无视觉，仅 4.5-turbo 系有）；ernie-4.5-turbo-vl 思考误判（实际无思考 32K） | 对照 ModelData/qianfan.json 修正（补 context 档、视觉仅 vl/turbo 版、-vl 无思考） | ✅ |
| B-06 | P2 | 模型识别 | doubao 家族：doubao-seed-1.6-flash 思考误判（实际不思考）；doubao-seed-translation 思考/工具误判（实际均不支持） | 对照 ModelData/volcengine.json 补 -flash/-translation 特例 | ✅ |
| B-07 | P3 | 模型识别 | gpt 家族：gpt-5.6-luna 上下文特例缺失（实际 270K，家族 gpt-5.6* 一律 922K） | 补 gpt-5.6-luna* 特例 | ✅ |
| B-09 | P3 | 上下文链路 | 128K 兜底三处硬编码：`EarlyTruncationFilter.DefaultContextLength` const、`TokenBudgetFilter.cs:52`（未注入 ChatSetting）、`MessageFlow.cs:1027/1169`（NAI ChatAI）；记忆声称 ChatSetting 曾有 DefaultContextLength 配置项但实际未实现，三处行为一致（0→128K、负→不限）无行为 bug | 统一为共享常量/配置项；行为一致暂不修（避免跨项目无谓耦合） | ⏸ 暂缓 |
| B-10 | P2 | 上下文链路 | 模型元数据 `MaxOutputTokens`（如 deepseek-v4 393216/claude 131072）仅采集展示，未用于主对话输出预算——主对话 `MessageFlow.InvokeLlmAsync` 未设 MaxTokens（`ChatRequest.Create(context.Options)` 默认 null），服务商自由生成，单次成本与超长风险不可控；是否补 `min(显式 MaxTokens, MaxOutputTokens)` 防线涉及产品行为（截断长文） | 待用户产品决策 | ⏸ 待确认 |
| B-11 | P3 | 上下文链路 | `ChatErrorHelper._patterns` 宽模式（`too many tokens`/`exceeds the maximum`/`token limit`）会把"max_tokens 参数超模型上限"类错误误判为 `CONTEXT_TOO_LONG`（错误建议"开启新会话"）；收紧恐漏报新厂商文案 | 权衡维持现状，登记 | ⏸ 暂缓 |
| B-12 | **P1** | 能力契约 | 两版网关多模态派发只认 `client is IXxxClient` 接口、未校验模型能力位 `config.SupportXxx`——OpenAI 兼容商（OpenAIChatClient/BuiltinChatClient 37+ 家）继承链实现全部多模态接口，纯对话模型（Cohere/Groq 等）被错误放行到多模态端点 → 请求打服务商不存在端点返回原始 4xx 而非清晰 `MODEL_UNSUPPORTED` | NAI ChatAI `GatewayController` 9 端点 + StarChat `GatewayController` 6 端点统一补 `!config.SupportXxx \|\| client is not IXxxClient`；图像生成（走 chat 路径无接口检查）补独立能力位守卫；任务状态查询 GET 不加 | ✅ |
| B-13 | **P1** | 协议客户端 | **Anthropic/Bedrock 流式工具调用缺失**：非流式工具完整，但流式事件解析对 tool_use 零处理——Anthropic `ToChunkResponse` 无 tool_use 分支（content_block_start(tool_use)+input_json_delta 均 return null）；Bedrock 流式事件模型无 toolUse/input 建模 → 流式工具整链丢失（ToolChatClient 流式路径无法执行工具） | 两客户端状态化 `_toolBlocks`（content block index → id/name）：tool_use 开始产出带 Id/Name 的首块，input_json_delta 产出参数分片块（消费端 `MergeToolCallDelta` 按 Id 合并追加）；Anthropic 加 `AnthropicDelta.PartialJson`，Bedrock 补 ContentBlockStart/toolUse 事件模型（`ContentBlockIndex` 对齐 Converse 字段）；每次流式调用清理状态 | ✅ |
| B-14 | P3 | 协议客户端 | finishReason 映射覆盖缺口：Anthropic `pause_turn`/`refusal`、Bedrock `guardrail_intervened`/`refusal`、Gemini `MALFORMED_FUNCTION_CALL`/`OTHER` 等未映射（返回 null）；FinishReason 枚举无对应值，前端仅知"停止"无具体原因 | 登记暂缓（FinishReason 枚举无对应值，需先扩展枚举语义） | ⏸ 暂缓 |
| B-15 | P3 | 协议客户端 | 三套映射一致性审计（Explore 全量）：Gemini/DashScope/OpenAI/Ollama 干净（共用转换助手）；Anthropic/Bedrock 的 `ToChatResponse` 与 getter 差异均为**无生产调用方的死代码漂移**（Text 首块 vs join 仅 trace 日志用）；**Bedrock usage cache 缺失为误判**——Converse API usage 标准字段仅 input/output/total，无 cache 字段（非实现遗漏） | 登记说明；`ToChatResponse` 死代码待统一收口（低优先） | ⏸ 暂缓 |
| B-08 | **P1** | 模型识别 | 推断入口未打通非 OpenAI 协议：`InferModelCapabilities` 在 OpenAIClientBase（public virtual），Anthropic/Gemini/Bedrock（继承 AiClientBase）走不到；消费侧 `client as OpenAIClientBase`（ChatAI ModelService L538/926/933、StarChat ModelMetadataService L82-88）对非 OpenAI 客户端落空 | 推断方法上提 AiClientBase（OpenAIClientBase 改 override 保二进制兼容），消费侧改 `as AiClientBase` | ✅ |

---

## 3. 分轮记录

### Phase 0（2026-09-05）— 基线建立

- `dotnet build NAI/NewLife.AI.sln` 0 错误；NewLife.AI 库代码 0 编译警告（2 个 SourceLink 环境警告非代码）；XUnitTest 445 警告基线（测试项目，Mcp 新文件 CS8632/xUnit 规则）
- 全量测试基线：**1936 通过 / 0 失败 / 7 跳过**（总计 1943）
- 旧修复在位抽样验证（5/6 在位）：
  - A-105（工具重复注册警告）✅ `ToolRegistry.cs:397` XTrace 日志在位
  - A-108（WS 多事件切分）✅ `DashScopeChatClient.WebSocket.cs` SplitJsonObjects/ParseJsonEvents/_pendingEvents 在位
  - R4-2（max_completion_tokens）✅ `ChatCompletionRequest.cs` MaxCompletionTokens/IsMaxCompletionTokensModel 在位
  - R6-1（Anthropic 流式 cache tokens）✅ `AnthropicResponse.cs:507-514` message_start 映射 cache 在位
  - R12-1（Ollama 转换一致性）✅ `OllamaChatResponse.cs` ToChatResponse/ToStreamChunk 在位
  - A-27（McpServer tools/call）→ 08-26 MCP 自包含重构已移除 Remoting，Phase 7 按重构后形态复核

### Phase 1（2026-09-05）— 模型家族引擎扩展 + 推断统一（校准后执行）

> **基线校准**：NAI submodule 已从探索基线 5466bfc 更新到 411f59c（含 e0131b1「模型能力家族化推断与价格分层重构」——家族已扩到 17 个且移除价格）。探索结论部分过时，本阶段转为**审计修正已家族化实现**。B-01~B-08 全部修复。

- **B-01 claude/gemini 家族**：`ModelFamilies.cs` 新增 `CreateClaude()`（claude* 全系 200K 思考+工具+视觉，opus-4-7 特例 1M）与 `CreateGemini()`（gemini* 全系 1M）并注册
- **B-02 glm 家族**：对照 ModelData/zhipu.json 修正——glm-5.x=1M、glm-5*-flash 不思考、glm-4.7=256K（-flash 特例 200704 不思考无视觉）、glm-4.5/4.6=128K、glm-4.5v/4.6v 不思考（4.5v=64K）
- **B-03 kimi**：kimi-k3=1M、kimi-k2.x vision=true（k2-0905 历史版特例 false）
- **B-04 minimax**：MiniMax-M3=1M
- **B-05 ernie**：补主档 128K context（DefaultContext 8K→131K）、5.x 主档无视觉、4.5-turbo 视觉、-vl 版不思考+32K、轻量档 32K
- **B-06 doubao**：seed-*-flash 不思考、seed-translation 无思考无工具
- **B-07 gpt**：补 gpt-5.6-luna=270K 特例
- **B-08 推断入口上提**：`AiClientBase` 新增 `#region 模型识别`（public virtual `InferModelCapabilities`/`InferModelDisplayName`：非对话提示词 → 家族 → 通用启发式）；`OpenAIClientBase` 改 `public override`（保留 OpenAI 专属启发式+价格探测，virtual 分派保多态与二进制兼容）；消费侧 ChatAI `ModelService.cs`（3 处）与 StarChat `ModelMetadataService.cs` 由 `as OpenAIClientBase` 改 `as AiClientBase`——**Anthropic/Gemini/Bedrock 客户端现自动获得家族推断**
- **测试**：ModelFamilyTests 更新 8 处过时断言（固化旧错误值）+ 新增 8 用例（glm 特例档/kimi-k3 1M/ernie-5.1/非 OpenAI 客户端继承推断 NonOpenAI_Clients_InheritFamilyInference 等）→ 118 项全绿
- **验证**：NAI 构建 0 错误；NAI 全量 **1944 通过 / 0 失败 / 7 跳过**（+8）；星语 `星语.sln` 构建 0 错误

### Phase 2（2026-09-05）— 模型特征识别链路审计

核查覆盖：三源元数据一致性、刷新/发现顺序、128K 兜底语义、MaxOutputTokens 输出预算、模型名归一化、超限识别误报、Locked 粒度、双份 ModelConfig。

- **排除项（探索疑似风险不成立）**：
  - 刷新顺序安全：`DataPreloadService` 启动序 = DoDiscoverAsync(L53) → RefreshModelCapabilitiesAsync(L63 家族/描述符粗值) → RefreshModelMetadata(L73 ModelData JSON 精确值**最后覆盖**)，粗值不会覆盖精确值
  - 模型名归一化：家族 glob 尾部 `*` + 描述符前缀匹配（`-`/`:` 边界）已覆盖日期变体；ModelData 13 表均纯版本号无 `-latest`
  - Locked 粒度（改任意字段即锁整行）：防误覆盖优先的保守设计，合理
  - ChatAI/StarChat 双份 ModelConfig 实体：两产品独立架构，刷新逻辑已分离，合理
- **登记权衡项**：B-09（128K 三处硬编码、ChatSetting 配置项缺失，行为一致暂缓）、B-10（MaxOutputTokens 未用于输出预算，产品决策待确认）、B-11（ChatErrorHelper 宽模式误报，权衡暂缓）
- 结论：本链路无 P1 级代码缺陷，运行期行为正确（守卫/超限识别/JSON 精确值覆盖均可靠）

### Phase 3（2026-09-05）— 能力契约层审计

- **接口 remarks 漂移更正**（P2）：`ISpeechClient`/`ITranscriptionClient`/`IImageClient` 的 remarks 把 **Azure 列入"已实现"**（实际 AzureAIChatClient 未实现），更正为"不实现（语音/图像走 Azure 独立服务）"；`IVideoClient`/`IRerankClient` 的 Gemini/Bedrock"（可选）"歧义措辞澄清为"官方支持但 SDK 未实现"
- **Azure 模型列表死实现修复**（P2）：AzureAIChatClient 继承基类 `ListModelsAsync` 请求 `/v1/models` 对 Azure 必然 404 → `OpenAIClientBase` 提取 `protected virtual BuildModelListUrl()`，Azure override 为 `/openai/models?api-version={ApiVersion}`；补单测 `BuildModelListUrl_AzureModelsEndpoint`
- **B-12 网关能力位校验**（P1）：NAI ChatAI `GatewayController` 9 端点 + StarChat `GatewayController` 6 端点补 `!config.SupportXxx` 前置校验（OpenAI 兼容商继承链致纯对话模型被误放行到多模态端点）
- **验证**：NAI 全量 **1945 通过 / 0 失败 / 7 跳过**（+1）；星语 `星语.sln` 构建 0 错误；星语网关集成测试 **23 失败为预存环境基线**（git stash 对比改动前后均 23 失败，依赖 AppKey/DB/真实服务商，与本轮改动无关）；NAI 版网关测试随全量通过

### Phase 4（2026-09-05）— 协议转换一致性审计

- **B-13 Anthropic/Bedrock 流式工具调用修复**（P1，本轮最大补充）：两客户端状态化 `_toolBlocks`，content_block_start(tool_use)/contentBlockStart 产出带 Id/Name 首块、input_json_delta/contentBlockDelta(toolUse.input) 产出参数分片块，消费端 `MergeToolCallDelta` 按 Id 合并为完整参数 JSON；Anthropic 加 `AnthropicDelta.PartialJson`；Bedrock 补完整流式工具事件模型（ContentBlockStart/ContentBlockStop/工具增量，**ContentBlockIndex 对齐 Converse 实际字段**）；每次流式调用清理状态
- **网关归一化补 Bedrock**：`NormalizeGatewayRequest` 加 `BedrockRequest → ToChatRequest` 分支（Bedrock 有 ToChatRequest 原未接线，A-94 统一化方案补全）；DashScope 原生请求场景罕见且无 ToChatRequest 暂不纳入
- **响应三套映射系统审计**（Explore 全 6 响应类）：Gemini/DashScope/OpenAI/Ollama 干净（共用转换助手）；Anthropic/Bedrock 的差异均在无生产调用方的 `ToChatResponse` 死代码路径；排除 Bedrock usage cache 误判（Converse 协议无该字段）；登记 B-14/B-15
- **测试**：Anthropic/Bedrock 流式工具 stub 测试各 1（参数分片跨事件累积为完整 JSON 断言）→ 两流式测试类 16 项全绿
- **验证**：NAI 全量 **1921 通过 / 0 失败 / 33 跳过**（跳过数 7→33 波动：RequiresApiKeyFact 集成测试依赖本机 Key 配置，Phase 3 时有 Key 现无——环境因素非代码回归，0 失败为硬指标）

---

---

（完）
