# 对话交互式 AI 系统 — 架构设计文档

> 版本：v0.4  
> 日期：2026-03-13  
> 关联文档：[需求规格说明 v0.8](需求规格说明.md)  

---

## 1. 功能清单与完成状态

### 1.1 后端功能

| # | 功能模块 | 功能项 | 状态 | 备注 |
|---|---------|--------|------|------|
| 1 | 会话管理 | 新建会话 | ✅ 完成 | |
| 2 | 会话管理 | 获取会话列表（分页） | ✅ 完成 | |
| 3 | 会话管理 | 更新会话（重命名、切换模型） | ✅ 完成 | |
| 4 | 会话管理 | 删除会话 | ✅ 完成 | |
| 5 | 会话管理 | 置顶/取消置顶 | ✅ 完成 | |
| 6 | 会话管理 | 标题自动生成 | ✅ 完成 | IChatApplicationService.GenerateTitleAsync，首条消息后异步生成 |
| 7 | 消息 | 发送消息（SSE 流式响应） | ✅ 完成 | InMemory + Db 实现 |
| 8 | 消息 | 获取会话消息列表 | ✅ 完成 | |
| 9 | 消息 | 编辑消息 | ✅ 完成 | |
| 10 | 消息 | 重新生成回复 | ✅ 完成 | 直接替换，不保留分支 |
| 11 | 消息 | 停止生成 | ✅ 完成 | |
| 12 | 消息 | 思考过程输出 | ✅ 完成 | MessagesController 使用 ChatStreamEvent SSE 事件模型，支持 thinking_delta/thinking_done 等事件 |
| 13 | 反馈 | 提交点赞/点踩 | ✅ 完成 | |
| 14 | 反馈 | 取消反馈 | ✅ 完成 | |
| 15 | 分享 | 创建共享链接 | ✅ 完成 | |
| 16 | 分享 | 获取共享对话内容 | ✅ 完成 | |
| 17 | 分享 | 撤销共享链接 | ✅ 完成 | |
| 18 | 附件 | 上传附件 | ✅ 完成 | 复用魔方附件体系 |
| 19 | 附件 | 下载/预览附件 | ✅ 完成 | |
| 20 | 模型 | 获取可用模型列表 | ✅ 完成 | |
| 21 | 用户设置 | 获取/更新设置 | ✅ 完成 | |
| 22 | 用户设置 | 导出所有对话 | ✅ 完成 | |
| 23 | 用户设置 | 清除所有对话 | ✅ 完成 | |
| 24 | 系统配置 | ChatSetting 配置类 | ✅ 完成 | 继承 Config\<ChatSetting\>，含分享有效期、默认模型、网关限流等系统级配置 |
| 25 | 数据持久化 | XCode 实体类 | ✅ 完成 | Model.xml 含全部 9 张表，实体类已生成 |
| 26 | 数据持久化 | DbChatApplicationService | ✅ 完成 | 基于 XCode 实体类实现，已注册为默认服务 |
| 27 | API 网关 | 统一 API 路由（OpenAI/Anthropic/Gemini） | ✅ 完成 | GatewayController + GatewayService，按 model 字段路由 |
| 28 | API 网关 | 多协议适配转换 | ✅ 完成 | IAiProvider + 33 个服务商适配类 |
| 29 | API 网关 | 上游 429 限流重试 | ✅ 完成 | GatewayService 指数退避 + 随机抖动，最多 5 次 |
| 30 | 函数调用 | Function Calling 处理 | ✅ 完成 | ToolCallService 编排服务，支持 think-tool-think 交错，最多 10 轮工具调用循环 |
| 31 | 函数调用 | 交错思考支持 | ✅ 完成 | ToolCallService + DbChatApplicationService 完整支持 thinking_delta/tool_call/content_delta 交错流 |
| 32 | MCP | MCP 工具调用 | ✅ 完成 | McpClientService，复用 NewLife.AI 的 MCP 协议模型，支持工具发现与工具调用 |
| 33 | MCP | MCP Server 管理 | ✅ 完成 | McpApiController，CRUD + 手动工具发现 + 工具列表查询 |
| 34 | 图像 | 图像生成 API | ✅ 完成 | GatewayController 路由到图像生成模型，解析 prompt/size 参数 |
| 35 | 图像 | 图像编辑 API | ✅ 完成 | GatewayController 解析 multipart/form-data，image/mask/prompt 编码为 base64 多模态消息调用模型 |
| 36 | 统计 | 用量记录与统计 | ✅ 完成 | UsageService + UsageApiController，支持按用户/按日/按模型查询 |
| 37 | 消息 | 后台继续生成 | ✅ 完成 | BackgroundGenerationService，后台消费事件流，支持任务状态查询 |
| 38 | API Key | AppKey CRUD 管理 | ✅ 完成 | AppKeyApiController，创建/列表/更新/删除，sk- 前缀随机密钥 |
| 39 | API Key | 网关请求 AppKey 认证 | ✅ 完成 | GatewayService.ValidateAppKey，Bearer Token 校验 |
| 40 | 统计 | 按 AppKey 维度用量统计 | ✅ 完成 | UsageService + UsageApiController，支持 /api/usage/appkeys 和 /api/usage/appkeys/{id}/daily |
| 41 | 技能 | 技能 CRUD 管理 | ❌ 待开发 | SkillController，系统技能不可删除 |
| 42 | 技能 | 用户技能使用记录 | ❌ 待开发 | 记录最近使用的技能，7 天内按使用时间排序 |
| 43 | 技能 | 技能激活注入 | ❌ 待开发 | @技能名 引用解析 + System Prompt 拼接 |
| 44 | 技能 | SkillBar 数据接口 | ❌ 待开发 | 返回"最近使用 + 系统技能"列表 |

### 1.2 前端功能

| # | 功能模块 | 功能项 | 状态 | 备注 |
|---|---------|--------|------|------|
| 1 | 布局 | 左右分栏布局 | ✅ 完成 | css/app.css + index.html，仿 Qwen 风格 |
| 2 | 布局 | 侧边栏收起/展开 | ✅ 完成 | sidebar.js，支持动画过渡 |
| 3 | 布局 | 响应式移动端适配 | ✅ 完成 | 768px/480px 断点，移动端 overlay 侧边栏 |
| 4 | 侧边栏 | 新建对话 | ✅ 完成 | sidebar.js，触发 chatai:newConversation 事件 |
| 5 | 侧边栏 | 对话列表（分组+滚动加载） | ✅ 完成 | sidebar.js，按今天/昨天/本周/本月/更早分组，滚动触底自动加载 |
| 6 | 侧边栏 | 会话操作（重命名/删除/置顶） | ✅ 完成 | sidebar.js，行内重命名 + 删除确认 |
| 7 | 侧边栏 | 个人图标与菜单 | ✅ 完成 | sidebar.js，弹出菜单含设置入口 |
| 8 | 设置页 | 通用设置 | ✅ 完成 | settings.js，语言/主题/字号/快捷键 |
| 9 | 设置页 | 对话设置 | ✅ 完成 | settings.js，默认模型/思考模式/上下文轮数/系统提示词 |
| 10 | 设置页 | 数据与隐私 | ✅ 完成 | settings.js，导出/清除/训练授权 |
| 11 | 主内容区 | 模型选择器 | ✅ 完成 | models.js，下拉选择 + 当前模型名展示 |
| 12 | 主内容区 | 新会话欢迎页 | ✅ 完成 | welcome.js，Logo + 推荐问题卡片 |
| 13 | 输入区 | 多行输入框（自动伸缩） | ✅ 完成 | composer.js，最大高度限制 |
| 14 | 输入区 | 附件上传 | ✅ 完成 | composer.js，点击/拖拽/粘贴三种方式 |
| 15 | 输入区 | 思考模式选择器 | ✅ 完成 | models.js，自动/思考/快速三档 |
| 16 | 输入区 | 发送/停止按钮 | ✅ 完成 | composer.js，生成中切换为停止按钮 |
| 17 | 对话区 | 消息气泡渲染 | ✅ 完成 | chat.js，用户/AI 双列 + 头像 |
| 18 | 对话区 | SSE 流式接收与渲染 | ✅ 完成 | sse.js + chat.js，实时增量渲染 |
| 19 | 对话区 | 思考过程折叠展示 | ✅ 完成 | chat.js，可折叠思考块 + 计时 |
| 20 | 对话区 | Markdown 渲染 | ✅ 完成 | markdown.js，代码高亮/Mermaid/KaTeX/复制按钮 |
| 21 | 对话区 | 消息操作（复制/编辑） | ✅ 完成 | chat.js，内联编辑 + 剪贴板复制 |
| 22 | 对话区 | AI 回复操作（点赞/点踩/重新生成） | ✅ 完成 | chat.js + feedback.js |
| 23 | 分享 | 创建并展示共享链接 | ✅ 完成 | share.js，生成链接 + 一键复制 |
| 24 | 分享 | 共享对话只读页面 | ✅ 完成 | chat.js loadSharePage()，只读浏览 + 引导注册 |
| 25 | 对话区 | 工具调用块展示（ToolCallBlock） | ✅ 完成 | chat.js，折叠式展示参数/结果/状态 |
| 26 | 对话区 | 交错思考展示 | ✅ 完成 | chat.js + sse.js，think–tool–think 完整链路 |
| 27 | 对话区 | 图像生成/编辑展示 | ✅ 完成 | chat.js Markdown 内联图片，lightbox 放大预览 |
| 28 | 对话区 | 全屏图片预览 | ✅ 完成 | lightbox.js，缩放 + 键盘导航 + 缩略图 |
| 29 | 设置页 | 使用量统计展示 | ✅ 完成 | settings.js，会话/消息/Token 统计卡片 |
| 30 | 主内容区 | 模型能力标识图标 | ✅ 完成 | models.js，🖼️👁️🧠🔧 能力标签 |
| 31 | 设置页 | AppKey 管理界面 | ✅ 完成 | settings.js，创建/列表/删除 + 密钥一键复制 |

---

## 2. 项目结构与依赖

### 2.1 项目信息

- 项目名称：`NewLife.ChatAI`
- 项目类型：ASP.NET Core Web
- 目标框架：`net8.0`
- 必须引用：`NewLife.Cube.Core`
- 前后端同项目：前端静态资源放 `wwwroot/`，API 放 `Controllers/`

系统分为两层：

| 层级 | 项目 | 职责 |
|------|------|------|
| 基础库 | `NewLife.AI` | 多协议模型适配（OpenAI/Anthropic/Gemini）、33 个服务商支持、函数调用、图像生成等通用能力。不依赖数据库 |
| 应用层 | `NewLife.ChatAI` | 对话 Web 前端、会话管理、API 网关路由、MCP 工具管理、使用量统计等业务功能。依赖 XCode 数据库 |

### 2.2 目录结构

```
NewLife.ChatAI/
├── Controllers/          # API 控制器
│   ├── ConversationsController.cs    # 会话管理
│   ├── MessagesController.cs         # 消息发送与流式响应
│   ├── FeedbackController.cs         # 点赞/点踩反馈
│   ├── ShareController.cs            # 分享功能
│   ├── AttachmentsController.cs      # 附件上传/下载
│   ├── ModelsController.cs           # 模型列表
│   ├── UserSettingsController.cs     # 用户设置
│   ├── GatewayController.cs          # API 网关路由（需求 §7）
│   ├── ImageController.cs            # 图像生成/编辑（需求 §6.11）
│   ├── UsageController.cs            # 使用量统计（需求 §10）
│   ├── AppKeyController.cs           # AppKey 管理（需求 §7.5）
│   └── McpApiController.cs           # MCP 管理（需求 §9）
├── Entity/               # XCode 实体类（由 Model.xml 生成）
│   ├── Config/
│   ├── Model.xml
│   ├── 会话.cs / 会话.Biz.cs
│   ├── 对话消息.cs / 对话消息.Biz.cs
│   ├── 消息反馈.cs / 消息反馈.Biz.cs
│   ├── 共享会话.cs / 共享会话.Biz.cs
│   ├── 模型配置.cs / 模型配置.Biz.cs
│   ├── 用户设置.cs / 用户设置.Biz.cs
│   ├── 用量记录.cs / 用量记录.Biz.cs
│   ├── 应用密钥.cs / 应用密钥.Biz.cs
│   └── MCP服务配置.cs / MCP服务配置.Biz.cs
├── Models/               # 模型类（由 xcode 生成）
├── Services/             # 业务服务层
│   ├── IChatApplicationService.cs    # 业务抽象接口
│   ├── InMemoryChatApplicationService.cs  # 内存版实现（开发期）
│   ├── DbChatApplicationService.cs        # 数据库版实现
│   ├── GatewayService.cs             # API 网关路由服务
│   ├── ToolCallService.cs            # 函数调用/MCP 工具调用服务
│   ├── UsageService.cs               # 用量统计服务
│   ├── McpClientService.cs            # MCP 客户端服务（工具发现与调用）
│   └── SkillService.cs               # 技能服务（@引用解析、System Prompt 构建、使用记录）
├── wwwroot/              # 前端静态资源
│   ├── index.html
│   ├── app.js
│   └── app.css
├── Program.cs
└── appsettings.json
```

### 2.3 AI 编排框架策略

- 可选引入 Microsoft Semantic Kernel（SK）或 MAF。
- 当前阶段以"可插拔"方式预留编排层接口（`IChatApplicationService`），避免与具体框架强耦合。
- 在未明确收益前，不作为本期强制依赖。

---

## 3. 数据模型

### 3.1 核心实体表

以下表定义在 `NewLife.ChatAI/Entity/Model.xml` 中，使用 XCode 实体框架。详细字段定义见 Model.xml，此处仅列出表清单。

| # | 表名（实体名） | 中文名 | 说明 |
|---|---------------|--------|------|
| 1 | Conversation | 会话 | 一次完整的多轮对话上下文 |
| 2 | ChatMessage | 对话消息 | 会话中的单条发言，包括用户消息和 AI 回复 |
| 3 | MessageFeedback | 消息反馈 | 用户对 AI 回复的点赞或点踩 |
| 4 | SharedConversation | 共享会话 | 通过链接分享的对话快照 |
| 5 | ModelConfig | 模型配置 | 后端接入的大语言模型配置，含提供商、接口地址、能力标识等 |
| 6 | UserSetting | 用户设置 | 用户的个性化配置（语言、主题、默认模型等） |
| 7 | AppKey | 应用密钥 | API 网关访问凭证，支持按 Key 维度用量统计（需求 §7.5） |
| 8 | UsageRecord | 用量记录 | 每次 AI 调用的 Token 消耗，关联用户和 AppKey（需求 §10） |
| 9 | McpServerConfig | MCP服务配置 | MCP Server 列表及工具发现信息（需求 §9） |
| 10 | ProviderConfig | 提供商配置 | AI 服务商的连接信息，一个协议类型可有多个实例 |
| 11 | Skill | 技能 | 可复用的 AI 行为指令，Markdown 格式的结构化提示文本（需求 §13） |
| 12 | UserSkill | 用户技能 | 记录用户最近使用的技能，用于 SkillBar 展示排序（需求 §13） |

> **注意**：不支持消息分支，ChatMessage 无 `ParentMessageId` 字段。编辑/重新生成直接替换当前消息。

### 3.2 附件存储

附件 **不** 使用独立的 `ChatAttachment` 表，而是复用 **魔方（NewLife.Cube）已有的 `Attachment` 表**。

魔方附件表关键字段：

| 字段 | 说明 |
|------|------|
| Id | Int64，雪花 ID |
| Category | 业务分类，填 `ChatAI` |
| Key | 业务主键，可存消息 ID |
| FileName | 原始文件名 |
| Extension | 扩展名 |
| Size | 文件大小 |
| ContentType | MIME 类型 |
| FilePath | 本地相对路径 |

使用方式：
- 上传附件时，`Category` 设为 `"ChatAI"`，`Key` 设为消息 ID（发送前为 `"temp_用户ID"`）。
- 消息的 `Attachments` 字段存储附件 ID 数组的 JSON，如 `[123456, 789012]`。
- 下载/预览通过魔方附件控制器，或封装为 `/api/attachments/{id}` 代理。

---

## 4. API 接口规范

### 4.1 统一错误响应格式

所有接口错误均返回以下 JSON 结构：

```json
{
  "code": "ERROR_CODE",
  "message": "人类可读的错误描述",
  "traceId": "链路追踪ID"
}
```

常用错误码：

| 错误码 | HTTP 状态码 | 说明 |
|--------|-----------|------|
| `CONVERSATION_NOT_FOUND` | 404 | 会话不存在 |
| `MESSAGE_NOT_FOUND` | 404 | 消息不存在 |
| `MODEL_UNAVAILABLE` | 503 | 模型服务不可用 |
| `CONTEXT_TOO_LONG` | 400 | 上下文超出模型限制 |
| `RATE_LIMITED` | 429 | 请求过于频繁 |
| `FILE_TOO_LARGE` | 413 | 附件超出大小限制 |
| `UNSUPPORTED_FILE_TYPE` | 415 | 不支持的文件类型 |
| `SHARE_EXPIRED` | 410 | 共享链接已过期 |
| `SHARE_NOT_FOUND` | 404 | 共享链接不存在 |
| `TOOL_CALL_FAILED` | 500 | 工具调用执行失败 |
| `MCP_SERVER_UNAVAILABLE` | 503 | MCP Server 不可达 |
| `IMAGE_GENERATION_FAILED` | 500 | 图像生成失败 |
| `INVALID_API_KEY` | 401 | AppKey 无效或已禁用 |
| `API_KEY_EXPIRED` | 401 | AppKey 已过期 |

### 4.2 对话管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/conversations | 新建会话 |
| GET | /api/conversations | 获取会话列表（分页，按 LastMessageTime 降序） |
| PUT | /api/conversations/{id} | 更新会话（重命名、切换模型等） |
| DELETE | /api/conversations/{id} | 删除会话 |
| PATCH | /api/conversations/{id}/pin | 置顶 / 取消置顶 |

### 4.3 消息

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/conversations/{id}/messages | 发送消息（返回 SSE 流式响应） |
| GET | /api/conversations/{id}/messages | 获取会话消息列表 |
| PUT | /api/messages/{id} | 编辑消息内容 |
| POST | /api/messages/{id}/regenerate | 重新生成回复（直接替换） |
| POST | /api/messages/{id}/stop | 中断生成 |

### 4.4 反馈

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/messages/{id}/feedback | 提交点赞 / 点踩反馈 |
| DELETE | /api/messages/{id}/feedback | 取消反馈 |

### 4.5 分享

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/conversations/{id}/share | 创建共享链接 |
| GET | /api/share/{token} | 获取共享对话内容（公开，无需认证） |
| DELETE | /api/share/{token} | 撤销共享链接 |

### 4.6 附件

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/attachments/upload | 上传附件，返回附件 ID 和 URL |
| GET | /api/attachments/{id} | 下载 / 预览附件 |

> 底层复用魔方 `Attachment` 实体，`Category = "ChatAI"`。

### 4.7 模型

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/models | 获取可用模型列表（仅返回已启用模型） |

### 4.8 用户设置

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/user/settings | 获取用户设置 |
| PUT | /api/user/settings | 更新用户设置 |
| POST | /api/user/export | 导出所有对话数据 |
| DELETE | /api/user/conversations | 清除所有对话 |

### 4.9 API 网关（需求 §7）

API 网关对外暴露兼容标准协议的接口，根据请求中的 `model` 字段自动路由到对应的模型提供商。

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /v1/chat/completions | OpenAI Chat Completions 兼容接口，支持流式/非流式 |
| POST | /v1/responses | OpenAI Response API 兼容接口（o3/gpt-5 等推理模型） |
| POST | /v1/messages | Anthropic Messages API 兼容接口 |
| POST | /v1/gemini | Google Gemini API 兼容接口 |
| POST | /v1/images/generations | 图像生成接口 |
| POST | /v1/images/edits | 图像编辑接口 |

> **路由策略**：根据 `model` 字段匹配 `ModelConfig` 表中的 `Code`，获取对应 `Provider`、`Endpoint`、`ApiKey`、`ApiProtocol`，由 NewLife.AI 基础库执行协议适配和请求转发。
>
> **限流重试**：上游返回 HTTP 429 时，在尚未产生任何输出前指数退避重试，最多 5 次。

### 4.10 使用量统计（需求 §10）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/usage/summary | 获取用户累计用量摘要（会话数、消息数、Token 总量） |
| GET | /api/usage/daily | 获取按日用量明细（支持起止日期查询） |
| GET | /api/usage/models | 获取各模型使用分布 |
| GET | /api/usage/appkeys | 获取各 AppKey 用量明细 |
| GET | /api/usage/appkeys/{id}/daily | 获取指定 AppKey 的按日用量 |

### 4.11 AppKey 管理（需求 §7.5）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/appkeys | 获取当前用户的 AppKey 列表 |
| POST | /api/appkeys | 创建 AppKey，返回密钥（仅展示一次） |
| PUT | /api/appkeys/{id} | 更新 AppKey（名称、启用状态、过期时间） |
| DELETE | /api/appkeys/{id} | 删除 AppKey |

### 4.12 MCP 管理（需求 §9）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/mcp/servers | 获取已配置的 MCP Server 列表 |
| POST | /api/mcp/servers | 添加 MCP Server |
| PUT | /api/mcp/servers/{id} | 更新 MCP Server 配置 |
| DELETE | /api/mcp/servers/{id} | 删除 MCP Server |
| POST | /api/mcp/servers/{id}/discover | 手动触发工具发现 |
| GET | /api/mcp/tools | 获取所有已发现的 MCP 工具列表 |

### 4.13 技能管理（需求 §13）

#### 技能 CRUD（管理员）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/skills | 获取所有技能列表，支持分页、分类筛选 |
| GET | /api/skills/{id} | 获取技能详情（含完整 Content） |
| POST | /api/skills | 创建新技能 |
| PUT | /api/skills/{id} | 更新技能（Version 自增） |
| DELETE | /api/skills/{id} | 删除技能（IsSystem=true 时禁止） |
| POST | /api/skills/preview | 传入 Content，解析 @引用 后返回完整展开文本 |

#### 用户技能

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/user/skills | 获取 SkillBar 展示列表（最近 7 天使用 + 系统技能，已排序） |
| PUT | /api/conversations/{id}/skill | 切换会话使用的技能 |

#### SkillBar 数据排序规则

`GET /api/user/skills` 返回列表的排序逻辑：

1. 查询 UserSkill 表中 `LastUseTime >= 7天前` 的记录，按 `LastUseTime DESC` 排列
2. 查询 Skill 表中 `IsSystem=true && Enable=true` 的记录，按 `Sort DESC` 排列
3. 合并去重（系统技能如已出现在用户最近使用中，不重复出现）
4. 前端 SkillBar 最多展示 6 个 + 1 个"更多"按钮

#### 技能激活时 System Prompt 构建

SkillService 负责在发送消息前构建完整的 System Prompt：

```
1. 拼接 IsSystem=true 且 Enable=true 的技能 Content
2. 拼接 UserSetting.SystemPrompt（用户全局提示词）
3. 解析当前消息中的 @技能名 引用，展开对应技能 Content
4. 拼接当前会话已激活的技能 Content
5. @引用 递归展开（最多 3 层，检测循环引用）
```

自动记录技能使用：激活技能时 upsert UserSkill 表，更新 `LastUseTime`。

---

## 5. SSE 流式协议规范

### 5.1 协议概述

发送消息接口 `POST /api/conversations/{id}/messages` 以 **Server-Sent Events（SSE）** 格式返回响应。

HTTP 响应头：

```
Content-Type: text/event-stream
Cache-Control: no-cache
Connection: keep-alive
```

每条 SSE 事件格式：

```
data: {JSON}\n\n
```

### 5.2 请求格式

```json
{
  "content": "用户输入的消息文本",
  "thinkingMode": 0,
  "attachmentIds": [123456, 789012]
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| content | String | 是 | 消息文本内容 |
| thinkingMode | Int32 | 否 | 0=自动(默认), 1=思考, 2=快速 |
| attachmentIds | Int64[] | 否 | 附件 ID 列表 |

### 5.3 SSE 事件类型

#### 5.3.1 消息头事件（message_start）

流开始时发送，包含消息的元信息：

```
data: {"type":"message_start","messageId":5001,"model":"qwen-max","thinkingMode":1}
```

| 字段 | 说明 |
|------|------|
| type | 固定 `"message_start"` |
| messageId | AI 回复的消息 ID |
| model | 实际使用的模型编码 |
| thinkingMode | 实际使用的思考模式 |

#### 5.3.2 思考过程增量事件（thinking_delta）

思考模式下，输出模型的推理过程：

```
data: {"type":"thinking_delta","content":"让我分析一下这个问题..."}
```

| 字段 | 说明 |
|------|------|
| type | 固定 `"thinking_delta"` |
| content | 思考内容的增量文本 |

#### 5.3.3 思考完成事件（thinking_done）

思考过程结束：

```
data: {"type":"thinking_done","thinkingTime":3200}
```

| 字段 | 说明 |
|------|------|
| type | 固定 `"thinking_done"` |
| thinkingTime | 思考耗时，毫秒 |

#### 5.3.4 内容增量事件（content_delta）

正式回答的增量内容：

```
data: {"type":"content_delta","content":"这是回答的一部分"}
```

| 字段 | 说明 |
|------|------|
| type | 固定 `"content_delta"` |
| content | 回答内容的增量文本 |

#### 5.3.5 完成事件（message_done）

全部内容输出完毕：

```
data: {"type":"message_done","usage":{"promptTokens":150,"completionTokens":320,"totalTokens":470}}
```

| 字段 | 说明 |
|------|------|
| type | 固定 `"message_done"` |
| usage | Token 用量统计（可选） |
| usage.promptTokens | 提示 Token 数 |
| usage.completionTokens | 回复 Token 数 |
| usage.totalTokens | 总 Token 数 |

#### 5.3.6 错误事件（error）

流式输出过程中发生错误：

```
data: {"type":"error","code":"MODEL_UNAVAILABLE","message":"模型服务暂时不可用，请稍后重试"}
```

| 字段 | 说明 |
|------|------|
| type | 固定 `"error"` |
| code | 错误码 |
| message | 错误描述 |

#### 5.3.7 工具调用开始事件（tool_call_start）（需求 §8.3）

工具调用 / MCP 调用开始时发送：

```
data: {"type":"tool_call_start","toolCallId":"call_001","name":"get_weather","arguments":"{\"city\":\"北京\"}"}
```

| 字段 | 说明 |
|------|------|
| type | 固定 `"tool_call_start"` |
| toolCallId | 本次调用的唯一标识 |
| name | 工具/函数名称 |
| arguments | 调用参数，JSON 字符串 |

#### 5.3.8 工具调用完成事件（tool_call_done）

工具调用返回结果：

```
data: {"type":"tool_call_done","toolCallId":"call_001","result":"{\"temp\":25,\"weather\":\"晴\"}","success":true}
```

| 字段 | 说明 |
|------|------|
| type | 固定 `"tool_call_done"` |
| toolCallId | 对应的调用标识 |
| result | 工具返回结果，JSON 字符串 |
| success | 是否成功 |

#### 5.3.9 工具调用失败事件（tool_call_error）

工具调用执行失败：

```
data: {"type":"tool_call_error","toolCallId":"call_001","error":"服务不可用"}
```

| 字段 | 说明 |
|------|------|
| type | 固定 `"tool_call_error"` |
| toolCallId | 对应的调用标识 |
| error | 错误描述 |

### 5.4 完整 SSE 流示例

#### 快速模式（无思考过程）

```
data: {"type":"message_start","messageId":5001,"model":"qwen-max","thinkingMode":2}

data: {"type":"content_delta","content":"你好！"}

data: {"type":"content_delta","content":"我是AI助手，"}

data: {"type":"content_delta","content":"有什么可以帮你的？"}

data: {"type":"message_done","usage":{"promptTokens":10,"completionTokens":15,"totalTokens":25}}
```

#### 思考模式（含思考过程）

```
data: {"type":"message_start","messageId":5002,"model":"deepseek-r1","thinkingMode":1}

data: {"type":"thinking_delta","content":"用户问的是一个数学问题，"}

data: {"type":"thinking_delta","content":"我需要用二次方程求根公式..."}

data: {"type":"thinking_delta","content":"判别式 Δ = b²-4ac = ..."}

data: {"type":"thinking_done","thinkingTime":3200}

data: {"type":"content_delta","content":"根据求根公式，方程的解为：\n\n"}

data: {"type":"content_delta","content":"$$x = \\frac{-b \\pm \\sqrt{b^2-4ac}}{2a}$$"}

data: {"type":"message_done","usage":{"promptTokens":50,"completionTokens":120,"totalTokens":170}}
```

#### 错误中断

```
data: {"type":"message_start","messageId":5003,"model":"gpt-4o","thinkingMode":0}

data: {"type":"content_delta","content":"让我来回答"}

data: {"type":"error","code":"MODEL_UNAVAILABLE","message":"模型连接超时，请稍后重试"}
```

#### 交错思考 + 工具调用（需求 §6.3.1 / §8）

```
data: {"type":"message_start","messageId":5004,"model":"gpt-4o","thinkingMode":1}

data: {"type":"thinking_delta","content":"用户想知道北京天气，我需要调用天气工具..."}

data: {"type":"thinking_done","thinkingTime":800}

data: {"type":"tool_call_start","toolCallId":"call_001","name":"get_weather","arguments":"{\"city\":\"北京\"}"}

data: {"type":"tool_call_done","toolCallId":"call_001","result":"{\"temp\":25,\"weather\":\"晴\"}","success":true}

data: {"type":"thinking_delta","content":"天气数据已获取，北京25度晴天，我来组织回复..."}

data: {"type":"thinking_done","thinkingTime":500}

data: {"type":"content_delta","content":"北京今天天气晴好，气温 25°C，"}

data: {"type":"content_delta","content":"适合户外活动。"}

data: {"type":"message_done","usage":{"promptTokens":80,"completionTokens":60,"totalTokens":140}}
```

### 5.5 前端处理逻辑

1. 收到 `message_start`：创建消息气泡，显示模型图标和加载指示器。
2. 收到 `thinking_delta`：在折叠区域追加思考过程文本，显示"思考中..."标题。
3. 收到 `thinking_done`：关闭思考区域，标题改为"思考过程（耗时 X.X 秒）"。
4. 收到 `tool_call_start`：在对话流中插入工具调用块（ToolCallBlock），显示工具名称、参数和旋转图标。
5. 收到 `tool_call_done`：更新工具调用块状态为成功（✓），显示返回结果（可折叠）。
6. 收到 `tool_call_error`：更新工具调用块状态为失败（✗），显示错误原因。
7. 收到 `content_delta`：在正式回答区域追加内容，进行 Markdown 增量渲染。
8. 收到 `message_done`：移除加载指示器，显示操作按钮，可展示 Token 用量。
9. 收到 `error`：停止流，显示错误提示，已输出内容保留。
10. 连接断开且未收到 `message_done` / `error`：视为异常中断，保留已有内容并提示。

> **交错渲染**：步骤 2–6 可交替出现多次（think–tool–think 链路），前端按事件到达顺序依次渲染即可。

---

## 6. 系统配置

### 6.1 ChatSetting 配置类

继承 `Setting<ChatSetting>`，提供系统级配置。配置文件默认位于 `Config/ChatSetting.config`。

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| ShareExpireDays | Int32 | 30 | 分享有效期。共享链接有效天数，0 表示永不过期 |
| DefaultModel | String | qwen-max | 默认模型。新用户的默认模型编码 |
| DefaultThinkingMode | Int32 | 0 | 默认思考模式。Auto=0, Think=1, Fast=2 |
| DefaultContextRounds | Int32 | 10 | 上下文轮数 |
| MaxAttachmentSize | Int32 | 20 | 最大附件大小（MB） |
| MaxAttachmentCount | Int32 | 5 | 单次最多上传附件数 |
| AutoGenerateTitle | Boolean | true | 首条消息后是否自动生成会话标题 |
| TitlePrompt | String | 请用10个字以内… | 标题生成提示词模板 |
| AllowedExtensions | String | .jpg,.jpeg,… | 允许的文件扩展名 |
| EnableGateway | Boolean | true | 启用 API 网关 |
| GatewayRateLimit | Int32 | 60 | 网关限流。每分钟每用户最大请求次数 |
| UpstreamRetryCount | Int32 | 5 | 上游重试次数。模型返回 429 时最大重试 |
| EnableFunctionCalling | Boolean | true | 启用函数调用 |
| EnableMcp | Boolean | true | 启用 MCP 工具调用 |
| DefaultImageSize | String | 1024x1024 | 图像生成默认尺寸 |
| EnableUsageStats | Boolean | true | 启用用量统计 |
| BackgroundGeneration | Boolean | true | 后台继续生成 |

### 6.2 配置使用说明

- `ChatSetting.Current`：全局获取当前配置实例。
- 修改配置后自动保存到 `Config/ChatSetting.config`。
- 系统启动时自动加载，不存在则创建默认配置文件。

---

## 7. 数据持久化策略

### 7.1 当前状态

- **InMemoryChatApplicationService**：内存版实现，用于前期开发和测试。
- **DbChatApplicationService**：XCode 数据库版实现（待开发）。

### 7.2 服务接口

通过 `IChatApplicationService` 接口抽象，DI 注入切换实现：

```csharp
// 开发阶段
services.AddSingleton<IChatApplicationService, InMemoryChatApplicationService>();

// 正式阶段
services.AddScoped<IChatApplicationService, DbChatApplicationService>();
```

### 7.3 XCode 实体使用

- 数据库操作使用 `NewLife.XCode`。
- 实体定义在 `Entity/Model.xml` 中，通过 `xcode` 命令生成。
- 数据库连接配置为 `ChatAI`，默认 SQLite，可切换 MySQL/PostgreSQL。

---

## 8. 标题自动生成策略

### 8.1 触发条件

1. 会话的 `MessageCount == 0`（首次发送消息时）。
2. 系统配置 `ChatSetting.AutoGenerateTitle == true`。
3. 会话标题为默认值"新建对话"。

### 8.2 实现流程

```
用户发送首条消息
    ├── 主流程：正常调用模型生成 AI 回复（SSE 流式）
    └── 异步任务：调用模型生成标题
         ├── 输入：用户首条消息内容
         ├── 提示词：ChatSetting.TitlePrompt + 消息内容
         ├── 使用模型：当前会话的模型（非流式调用）
         ├── 成功：更新会话标题
         └── 失败：保持默认标题，不影响主流程
```

### 8.3 前端交互

- 首条消息发送后，通过 SSE 事件或轮询获取会话标题更新。
- 建议在 `message_done` 事件中附加 `title` 字段（仅首条消息时返回）：

```
data: {"type":"message_done","usage":{...},"title":"关于量子计算的讨论"}
```

---

## 9. 设计决策记录

| # | 决策 | 理由 |
|---|------|------|
| 1 | 不支持消息分支 | 简化前后端复杂度，编辑/重新生成直接替换 |
| 2 | 不做模型降级 | 模型不可用时直接报错，避免用户不知情使用非预期模型 |
| 3 | 复用魔方附件表 | 避免重复建设附件管理，魔方附件表已有完善的存储和管理能力 |
| 4 | 登录鉴权由外部模块提供 | 职责分离，NewLife.Cube 已有完整的用户和权限体系 |
| 5 | 共享有效期由后台配置 | 统一管理，避免每个用户自行设置导致管理困难 |
| 6 | SSE 区分思考/回答事件 | 前端可独立渲染思考过程折叠区域和正式回答 |
| 7 | 标题异步生成 | 不阻塞消息响应流程，用户体验更好 |
| 8 | 两层架构分离 | NewLife.AI（基础库）不依赖数据库，可单独使用；ChatAI（应用层）负责业务逻辑和持久化 |
| 9 | 不使用 Docker 沙箱执行代码 | 避免引入 Docker 依赖，降低部署复杂度 |
| 10 | 暂不含计费 | 先聚焦核心 Agent 能力，计费后续迭代；AppKey 用量统计已支持 |
| 11 | MCP 工具调用复用函数调用事件 | SSE 事件和前端组件统一，降低复杂度 |
| 12 | 技能用 Markdown 文本而非结构化配置 | 门槛最低，与 Cursor/Copilot 的 .instructions.md 思路一致 |
| 13 | 技能中以自然语言描述 MCP 工具使用 | 无需维护技能与 MCP Server 的 ID 关联，模型根据语义自行决策 |
| 14 | UserSkill 记录最近使用而非偏好配置 | 减少用户配置负担，SkillBar 自动反映使用习惯 |
| 12 | 后台继续生成 | 浏览器关闭不中断模型生成，用户重新打开可看到完整结果 |
| 13 | API 网关透明路由 | 按 model 字段自动匹配提供商，调用方无需关心具体后端 |
| 14 | AppKey 双维度统计 | 用量同时归属用户和 AppKey，支持双维度聚合查询 |

---
