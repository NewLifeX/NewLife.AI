# NewLife.AI 基础库全景审计

> 版本：v1.0 | 日期：2026-08-04
> 范围：`NAI/NewLife.AI/` 全部 12 维度逐目录扫描。协议客户端 chat/stream/think 维度已完成 12 轮分析（见 [协议客户端分析](协议客户端分析.md)），本审计重点覆盖其余维度与协议客户端残留。

---

## 1. 概述

本审计对 `NewLife.AI` 基础库做全景逐目录扫描，目标是**确保这个 NewLife AI 生态核心基础库无瑕疵**。发现问题登记到本文档，通过 dev-loop 分轮修复，全部闭合。

**验证基线**：`NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1495 通过 / 0 失败 / 7 跳过**（跳过为未配置 Key 的集成测试）。基线测试 1466，本审计新增 29 个测试。

> **第 5 轮（2026-08-04）补充**：NewLife.AI 库自身编译警告 110 → **0**（CS8604×62/8602×20/8619×12/8601×4/1574×6/1573×6 全部清除）；Slack/Telegram Markdown 转换方法改 internal 并补 11 个纯逻辑测试。全量测试 1484 → 1495。

---

## 2. 维度总览

| 编号 | 维度 | 目录 | 审计状态 | 问题数 |
|:----:|------|------|:--------:|:------:|
| D1 | 协议客户端（残留） | `Clients/` | ✅ 已审已修 | 15 |
| D2 | 工具调用引擎 | `Tools/` | ✅ 已审已修 | 2 |
| D3 | IM 渠道 | `Channels/` | ✅ 已审已修 | 9 |
| D4 | 智能体框架 | `Agents/` | ✅ 已审已修 | 8 |
| D5 | 编程智能体 | `Coding/` | ✅ 已审已修 | 7 |
| D6 | 嵌入 | `Embedding/` | ✅ 已审已修 | 2 |
| D7 | 过滤器 | `Filters/` | ✅ 已审（无问题） | 0 |
| D8 | Handler 链 | `Handlers/` | ✅ 已审已修 | 1 |
| D9 | 业务接口契约 | `Interfaces/` | ✅ 已审已修 | 1 |
| D10 | 记忆存储 | `Memory/` | ✅ 已审（无问题） | 0 |
| D11 | MCP | `ModelContextProtocol/` | ✅ 已审已修 | 14 |
| D12 | 模型 DTO / 服务 / 规划器 | `Models/` + `Services/` + `Planner/` | ✅ 已审已修 | 13 |

> 状态标记：✅ 已闭合 | 🟡 部分闭合 | ⏳ 待审 | 🔧 修复中

---

## 3. 问题登记表

> 严重级：**P0** 崩溃/计费错误/安全 | **P1** 功能缺陷/一致性 | **P2** 代码卫生/健壮性/性能
> 状态：🆕 已登记 | 🔧 修复中 | ✅ 已修复 | ⏸ 暂缓

| 编号 | 严重级 | 维度 | 问题 | 修复方案 | 状态 |
|:----:|:------:|:----:|------|----------|:----:|
| A-01 | P1 | D3 Channels | `QQChannel.SendMessageAsync` 每次请求 `using var httpClient = new HttpClient()`（连接泄漏反模式），其余渠道均用 `_clients` 缓存 `ApiHttpClient` | 统一为按 botToken 缓存 `HttpClient` 实例 | ✅ |
| A-02 | P1 | D3 Channels | `DiscordChannel` 用 `static readonly HttpClient _http = new()`，与其余渠道缓存模式不一致 | 统一为按 botToken 缓存 `HttpClient` 实例 | ✅ |
| A-03 | P1 | D1 Clients | `BedrockChatClient.SetHeaders` 用 `ReadAsStringAsync().GetAwaiter().GetResult()` 同步阻塞读取请求体（潜在死锁/线程池饥饿风险） | 分析确认 StringContent 内存读取返回已完成 Task 不阻塞线程；同步签名限制下为安全标准做法 | ⏸ 暂缓 |
| A-04 | P2 | D3 Channels | 11 个渠道仅 1 个测试文件（`ChannelTests`），覆盖薄 | 按渠道补纯逻辑测试（微信/QQ/Discord/Telegram/Slack/WeComBot + 原有 4 渠道，共 12 个新测试） | ✅ |
| A-05 | **P0** | D3 Channels | `WeChatMpChannel.SendMessageAsync` 中 `var secret = "";` 硬编码空字符串，`GetAccessTokenAsync(appId, "")` 必然失败，公众号消息发送功能**完全不可用**；注释声称「由外部服务注入缓存」但 `_tokenCache` 是 private static 无注入机制 | 增加 `AppSecret` 可配置属性，空值时明确报错 | ✅ |
| A-06 | **P0** | D3 Channels | `WeChatKfChannel.SendMessageAsync` 同样 `var secret = "";` 硬编码空字符串，微信客服发送功能**完全不可用** | 同 A-05（增加 `AppSecret` 属性） | ✅ |
| A-07 | P2 | D3 Channels | `QQChannel._tokenCache` 只写不读（`_tokenCache[botAppId] = botToken` 后从未读取），死代码 | 删除死代码（随 A-01 重构） | ✅ |
| A-08 | P2 | D3 Channels | `WebhookChannel.ValidateConfigAsync` 恒返回 `true`，配置校验形同虚设 | 校验为合法 http/https URL；更新测试 | ✅ |
| A-09 | P2 | D3 Channels | `WeChatMpChannel`/`WeChatKfChannel` 用字符串搜索解析 JSON（`IndexOf("\"access_token\":\"")`），脆弱易错 | 改用 `ToJsonEntity` 结构化解析 | ✅ |
| A-10 | P2 | D2 Tools | `ToolSchemaBuilder.BuildTypeSchema` 对复杂类型递归展开无深度限制，极端嵌套类型可能栈溢出 | 增加递归深度上限 `MaxDepth=8` | ✅ |
| A-11 | P2 | D2 Tools | `ToolHelper.IsSsrfRisk` 只检查字面 IP/主机名，不解析 DNS（`example.com` 等域名返回 false，域名指向内网时绕过 SSRF 防护） | 增加 DNS 解析检查 + IPv6 私网段 + 进程级缓存；补测试 | ✅ |
| A-12 | **P1** | D4 Agents/Coding | `CodingPlan.ToJson()` 方法体调用 `this.ToJson()`，C# 成员查找优先于扩展方法，构成**无限递归**，调用即 StackOverflow | 改为显式调用 `JsonHelper.ToJson(this)`；FromJson 补 null 防护（A-24 合并） | ✅ |
| A-13 | **P1** | D4 Agents/Coding | `CodingTools` 工作区沙箱 `StartsWith` 前缀匹配**未校验目录分隔符边界**，`C:\X\StarChat2\...` 等前缀相同兄弟目录被判定为工作区内，沙箱可绕过 | 校验前缀后为目录分隔符或精确相等；补测试 | ✅ |
| A-14 | P2 | D4 Agents/Coding | `Agents/FunctionCallingPlanner` 重规划逻辑：第二次遍历步骤失败直接 `break`，`MaxReplanning`（默认 2）实际最多只发生 1 次重规划，预算浪费 | 二次遍历失败也接入重规划循环 | ✅ |
| A-15 | P2 | D4 Agents/Coding | `ConversableAgent.MaxAutoReply` 只写不读的死属性，注释宣称的"自动工具循环"未实现 | 删除；更新类注释与测试 | ✅ |
| A-16 | P2 | D4 Agents/Coding | `ParallelGroupChat` 所有并行 worker 共享同一 `List<AgentMessage>` 引用，worker 改写历史时并发竞态 | 每个 worker 持有独立副本 | ✅ |
| A-17 | P2 | D4 Agents/Coding | `ParallelGroupChat` 裸 `catch { }` 吞异常无日志，工作代理失败原因完全不可见 | XTrace 记录异常与超时 | ✅ |
| A-18 | P2 | D4 Agents/Coding | `CodingTools` 池化 `StringBuilder` 在超时/异常路径未归还（`RunCommandAsync`/`GetErrorsAsync`），对象池实例流失 | 所有路径归还 | ✅ |
| A-19 | P2 | D4 Agents/Coding | `CodingTools.RunCommandAsync` 硬编码 `cmd.exe`，Unix/macOS 上 `run_command` 失效 | 按 `Environment.OSVersion` 分支 | ✅ |
| A-20 | P2 | D4 Agents/Coding | `CodingTools.GetErrorsAsync` 魔法数字 `120_000` 与 `RunCommandAsync` 的 `600_000` 超时上限不一致 | 抽常量 `MaxCommandTimeoutMs` 统一 | ✅ |
| A-21 | P2 | D4 Agents/Coding | `CodingTools` 搜索遇无权限目录 `UnauthorizedAccessException` 被捕获后**整个搜索返回 Error** 而非跳过继续 | 新增 `EnumerateFilesSafe` 递归跳过无权限目录 | ✅ |
| A-22 | P2 | D4 Agents/Coding | `CodingTools.ReadFileAsync` 行范围截取仍 `File.ReadAllLines` 全量读文件，大文件浪费 | 改用 `File.ReadLines` 惰性按需读取 | ✅ |
| A-23 | P2 | D4 Agents/Coding | `CodingAgent._currentPhase` 实例级共享可变状态，工具回调线程并发读写竞态；异常退出时残留上一阶段名 | **误报关闭**：`String` 引用读写原子无撕裂，阶段名仅展示用途；实例级锁也无法解决并发语义，已回滚 | ✅ |
| A-24 | P2 | D4 Agents/Coding | `CodingPlan.FromJson` 无 null 入参防护，json 为 null 时 NRE | 加 null 校验（随 A-12 修复） | ✅ |
| A-25 | P2 | D4 Agents/Coding | `CopilotSkillLoader.MatchInstructions` 无调用方，且 `MatchGlob("Doc/**")` 相对 glob 匹配绝对路径必然失败 | 修正 `MatchGlob` 支持绝对路径 + `**/` 前缀 | ✅ |
| A-26 | P2 | D4 Agents/Coding | `Agents/FunctionCallingPlanner.cs:287` 死代码：`var raw = json.TrimStart('[').TrimEnd(']');` 从未使用 | 删除 | ✅ |
| A-27 | **P1** | D11 MCP | `McpServer._handler` 与 `Manager` 完全断连（`new McpHandler()` 孤儿实例 Host 恒 null），`IApiManager` 从未注册，**每一次 `tools/call` 都返回 NotFound**，工具调用必然失败 | 继承 `ApiHost` 使 `_handler.Host=this`，`GetService(IApiManager)` 返回 `Manager`；配套修复 A-29/30/37 | ✅ |
| A-28 | P2 | D11 MCP | `McpServer` catch 分支从不写日志（类实现 ILogFeature 却不用），内部异常 `ex.Message` 原样回写客户端泄露实现细节 | 写日志（A-27 继承 ApiHost 后自动获得） | ✅ |
| A-29 | P2 | D11 MCP | `McpServer` 对 JSON-RPC notification（`notifications/initialized` 无 Id）回响应，违反 JSON-RPC 2.0 规范；测试还把该错误行为固化为断言 | 区分 notification 不响应；更新测试 | ✅ |
| A-30 | P2 | D11 MCP | `McpServer` 生成的 `SessionId` 被丢弃（`Rand.NextString(16)` 后从未回写），会话握手机制名存实亡 | 将新 SessionId 写入响应头 | ✅ |
| A-31 | P2 | D11 MCP | `McpServer.Process` 全链路同步阻塞异步（`_handler.Execute` 内部 `GetAwaiter().GetResult()`），工具为 async Task 时线程池饥饿风险 | 保持同步 API（公共签名）；内存 body 读取不阻塞线程 | ⏸ 暂缓 |
| A-32 | P2 | D8 Handlers | `ChatHandlerChain` 懒缓存 `??=` 写入无锁，读线程构建期间另一线程 `Invalidate` 置 null，旧列表写回后**缓存永久丢失新处理器** | `GetHandlers` 锁内双检 | ✅ |
| A-33 | P2 | D11 MCP | `McpServer` IProgress 支持名存实亡：工具内解引用 progress 即 NRE，`ProgressNotification`/`ProgressParams` 为死代码 | 删除 `ProgressNotification`/`ProgressParams` 死代码；IProgress 为框架限制，工具不应依赖进度 | ✅ |
| A-34 | P2 | D11 MCP | `McpServer` 协议校验缺失：未校验 `JsonRpc == "2.0"`、未区分 notification、`tools/call` 无鉴权与会话绑定 | 补 JsonRpc 版本校验；notification 不响应（A-29） | ✅ |
| A-35 | P2 | D11 MCP | `HttpMcpServer` 的 `context.Connection!` 可空强制解引用 NRE 风险 | 判空 + XTrace 记录 | ✅ |
| A-36 | P2 | D11 MCP | `HttpMcpServer`/`AspNetMcpServer` 请求体无大小限制，超大 body 可打爆内存（DoS） | `HttpMcpServer` 增加 1MB body 上限 | ✅ |
| A-37 | P2 | D11 MCP | `McpToolManager.ToSnakeCase` 对连续大写缩写处理错误（`GetAPIKey` → `get_apikey`），`APIKey`/`Apikey` 碰撞成同一工具名 | 修正缩写处理 | ✅ |
| A-38 | P3 | D11 MCP | `ToolInfo` 空类死代码，全库无引用 | 删除 | ✅ |
| A-39 | P3 | D11 MCP | `ProtocolModel` 的 `InitializeParams`/`ProgressNotification`/`ProgressParams` 三个 record 死代码 | 删除 `ProgressNotification`/`ProgressParams`（`InitializeParams` 被 ChatAI 引用保留） | ✅ |
| A-40 | P3 | D11 MCP | `McpServer.AddTool<TTools>` 参数从未使用；`MapMcp<TTools>` 端到端从未调用 `AddTool`，工具列表恒为空 | `MapMcp` 创建 server 后调用 `AddTool<TTools>` | ✅ |
| A-41 | P3 | D11 MCP | `McpContext.HostContext` 只写不读 | 保留（供 HTTP 宿主传上下文，属扩展点） | ⏸ 暂缓 |
| A-42 | **P1** | D12 Planner | `Planner/FunctionCallingPlanner.ExecuteAsync` 内层 `catch (Exception)` 吞掉 `OperationCanceledException`，取消被记录为 Failed 而非取消；外层 `catch (OperationCanceledException)` 死代码 | 内层优先 `catch (OperationCanceledException) { throw; }`；补测试 | ✅ |
| A-43 | **P1** | D12 Embedding | `VectorData.ToVector()` 对脏数据无保护：非法 base64 抛 `FormatException`，长度非 4 倍数时 `BlockCopy` 抛 `ArgumentException`，RAG 读取脏数据即崩溃 | `BlockCopy` 拷贝长度截断 + 非法 base64 返回空数组；补测试 | ✅ |
| A-44 | P2 | D12 Planner | `Planner/FunctionCallingPlanner._lock` 声明但从未使用（死代码），`ExecuteAsync` 并发修改 `Status` 无同步 | 删除 `_lock` | ✅ |
| A-45 | P2 | D12 Planner | `IPlan.SetFinalAnswer` 无任何调用者，`IPlan.FinalAnswer` 永远为 null，接口承诺未实现 | `ExecuteAsync` 完成后填充 `FinalAnswer`（最后成功步骤结果） | ✅ |
| A-46 | P2 | D12 Services | `BackgroundGenerationService` 延迟清理仅按 messageId 匹配，同 Id 新任务窗口内注册会被旧清理回调误清空 | 校验 `removed == task`（ReferenceEquals） | ✅ |
| A-47 | P2 | D12 Services | `BackgroundGenerationService` 重复注册同 messageId 时旧 CTS 泄漏（不 Cancel/Dispose） | 注册前 TryRemove 并取消旧条目 | ✅ |
| A-48 | P2 | D12 Embedding | `HashTextEmbedder` 类注释写 `local-hash-v1`，实现为 `local-hash-v2`，`VectorData` 注释也全写 v1，命名不一致导致 v1→v2 迁移语义误判 | 统一注释为 v2 | ✅ |
| A-49 | P2 | D9 Interfaces | `IAppKey.ExpireTime`/`IUserMemory.ExpireTime` 类型为 `DateTime` 但注释写"null表示永不过期"，契约矛盾 | 修正注释为 `DateTime.MinValue` 表示永不过期（XCode 实体值类型约束） | ✅ |
| A-50 | P2 | D6 Embedding | `EmbeddingClientMetadata.ProviderName = null!` init-only 属性无保护，外部构造后不赋值即使用 NRE | 改为构造器注入强约束 | ✅ |
| A-51 | P2 | D12 Services | `MessageRateLimiter` 注释称"后台定期清理"，实现仅在 `count % 200 == 1` 惰性清理，请求停止后过期桶永久残留 | 修正注释与实现一致 | ✅ |
| A-52 | **P1** | D12 Models | `ChatResponse.Text` 空内容列表（`[]`，tool_calls/content_filter 结束场景）时 `list.FirstOrDefault()` 返回 null，落到 `value.ToString()` 抛 NRE | `FirstOrDefault()` 后再判空；补测试 | ✅ |
| A-53 | **P1** | D12 Models | `ChatRequest.Create` 与 `ToolChatClient.MergeToolOptions` 对 `Items` 字典**按引用别名共享**，`DashScopeChatClient` 写 `request["EnableWebExtractor"]` 直接污染调用方原始 request；工具循环多轮残留；并发共享时非线程安全 | `ChatRequest.Create` 逐项复制 Items + `ToolChatClient.MergeToolOptions` 复制字典 | ✅ |
| A-54 | P2 | D12 Models | `ChatRequest.Create` else 分支为不可达死代码（新建 request.Items 恒为空字典，恒真短路） | 改为无条件逐项复制（随 A-53 修复） | ✅ |
| A-55 | P2 | D12 Models | `ChatOptions`/`EmbeddingModelSetting` 索引器未防御 `Items` 为 null，与 `ChatMessage` 索引器防御不一致 | 统一 `get` 判空 / `set` `??=` 创建 | ✅ |
| A-56 | P2 | D12 Models | `ChatMessage.ParseMultimodalContent` catch 吞异常无日志无埋点，解析失败排障无据 | 补 XTrace 日志 | ✅ |
| A-57 | P2 | D12 Models | `ChatResponse.AddToolCallDelta` 硬编码 `Index = 0`，与 `Add`/`AddDelta` 用 `msgs.Count` 不一致，多工具流式块产生多个 Index=0 的 choice | 用 `msgs.Count` | ✅ |
| A-58 | **P1** | D1 Clients | `DashScopeRealtimeClient.RealtimeEvent.Parse` 用抛异常字典索引访问可选键（`dic["response"]`/`dic["item_index"]`），首个 `session.created` 事件即抛 KeyNotFoundException，**接收循环必然中断** | 全部改用 `TryGetValue` | ✅ |
| A-59 | P2 | D1 Clients | `AiClientBase.Dispose()` 空实现，HttpClient（含 handler/连接池）从不释放，频繁 CreateClient 时连接堆积 | Dispose 释放内部创建的 HttpClient（`_ownsHttpClient` 区分外部注入） | ✅ |
| A-60 | P2 | D1 Clients | `AiClientRegistry` 两个 Dictionary 无锁并发，`Default` 公共可变单例运行时并发 Register+查询抛异常 | 改用 `ConcurrentDictionary` | ✅ |
| A-61 | P2 | D1 Clients | `DashScopeRealtimeClient._cts` 死状态：Token 从未被订阅，Dispose 的 `_cts?.Cancel()` 空操作，接收循环只能靠非预期异常中断 | 接收循环链接 `_cts.Token` | ✅ |
| A-62 | P2 | D1 Clients | `DashScopeRealtimeClient.ConnectAsync` 连接失败泄漏 `ws` | try/catch 中 `ws.Dispose()` | ✅ |
| A-63 | P2 | D1 Clients | `ChatClientBuilder.SetInnerClient` 静默覆盖已有内层客户端且不释放，配置被吞无告警 | 覆盖前释放旧客户端（非同一实例） | ✅ |
| A-64 | P2 | D1 Clients | `ChatCompletionRequest.ToChatRequest` 与 `FromChatRequest` 不对称：丢 `Name`/`ToolCallId`/`ReasoningContent`，tool 消息回送 400 / DeepSeek 思考回传违反约束 | 补三字段复制 | ✅ |
| A-65 | P3 | D1 Clients | `AiClientRegistry` 同 Code 重注册且 DisplayName 变化时 `_displayNames` 旧条目残留 | 重注册时清理旧 DisplayName | ✅ |
| A-66 | P3 | D1 Clients | `AiClientRegistry.Register(Assembly)` 的 `assembly.GetTypes()` 遇 `ReflectionTypeLoadException` 未处理 | try-catch 降级用异常子集 | ✅ |
| A-67 | P3 | D1 Clients | `AiClientBase.GetResponseAsync`/`GetStreamingResponseAsync` 直接改写调用方 `request.Stream`/`request.Model`，同一 request 先流式后非流式互相翻转 | 临时改写 + finally 恢复 | ✅ |
| A-68 | P3 | D1 Clients | `AiClientBase.CombineApiUrl` public API 对无前导 `/` 的 path 拼出畸形 URL | 规范化 path（无前导斜杠时补 `/`） | ✅ |
| A-69 | P3 | D1 Clients | `FilteredChatClient` 火焰即忘 `Task.Run` 内 `catch { }` 空吞 `OnStreamCompletedAsync` 异常，后处理失败不可见 | XTrace 记录异常 | ✅ |
| A-70 | P3 | D1 Clients | `ChatCompletionRequest.BuildBody`/`FromChatRequest` 只读 `request.MaxTokens`，忽略 `MaxCompletionTokens`，该属性经 DashScope/DeepSeek 路径被静默丢弃 | 确认 R4-2 已覆盖三路径（FromChatRequest/BuildBody/ToChatRequest），误报 | ✅ |
| A-71 | P3 | D1 Clients | `DashScopeRealtimeClient` 单条 WS 消息含多个 JSON 事件时解析失败被静默丢弃 | 需精细 JSON 流解析，可能引入新错误路径；DashScope 实时协议通常一条消息一个事件 | ⏸ 暂缓 |
| A-72 | P3 | D1 Clients | `AiClientBase._host` 非 readonly 可变静态状态 | 改 readonly | ✅ |

---

## 4. 扫描记录

（按维度记录扫描过程与发现）

### D3 Channels — 扫描记录

- 11 个渠道（DingTalk/WeCom/Feishu/Webhook/Slack/Telegram/Discord/QQ/WeChatMp/WeChatKf/WeComBot）均实现 `IMessageChannel`
- 大部分渠道用 `_clients` ConcurrentDictionary 缓存 `ApiHttpClient`，但 **QQChannel 每次新建 HttpClient**、**DiscordChannel 用静态 HttpClient**
- 测试仅 `XUnitTest/Channels/ChannelTests.cs` 覆盖 WeCom/DingTalk/Feishu/Webhook，其余 7 个渠道无测试
- `SlackChannel.ConvertToSlackMarkdown` 用正则转换 Markdown，有纯逻辑可测
- `TelegramChannel` 的 `GetClient` 按 botToken 缓存

---

## 5. 修复记录

### 第 1 轮（2026-08-04）— P0/P1 修复

| 编号 | 修复内容 | 涉及文件 |
|:----:|----------|----------|
| A-05/A-06 | 微信渠道 `secret` 硬编码空串 → 增加 `AppSecret` 可配置属性，空值时明确报错 | `Channels/WeChatMpChannel.cs`、`WeChatKfChannel.cs` |
| A-12/A-24 | `CodingPlan.ToJson()` 无限递归 → 显式 `JsonHelper.ToJson(this)`；`FromJson` 加 null 防护 | `Coding/Models/CodingPlan.cs` |
| A-13 | 沙箱 `StartsWith` 前缀碰撞绕过 → 校验目录分隔符边界 + 补测试 | `Coding/CodingTools.cs` + `CodingToolsTests.cs` |
| A-27/A-29/A-30/A-34 | `McpServer` 继承 `ApiHost` 绑定 `_handler.Host`，`GetService(IApiManager)` 返回 Manager，`tools/call` 不再 NotFound；notification 不响应；新 SessionId 回写响应头；校验 JsonRpc 2.0 | `ModelContextProtocol/McpServer.cs` |
| A-42 | Planner 取消令牌被吞 → 内层优先 `catch (OperationCanceledException) { throw; }` + 补测试 | `Planner/FunctionCallingPlanner.cs` + `PlannerTests.cs` |
| A-43 | `VectorData.ToVector()` 脏数据崩溃 → BlockCopy 截断 + 非法 base64 返回空数组 + 补测试 | `Embedding/VectorData.cs` + `VectorDataTests.cs` |
| A-52 | `ChatResponse.Text` 空内容数组 NRE → `FirstOrDefault()` 后再判空 + 补测试 | `Models/ChatResponse.cs` + `ChatCompletionResponseTests.cs` |
| A-53/A-54 | `ChatRequest.Create`/`ToolChatClient.MergeToolOptions` Items 别名共享污染 → 逐项复制 | `Models/ChatRequest.cs`、`Tools/ToolChatClient.cs` |
| A-58 | `RealtimeEvent.Parse` 字典索引崩溃 → 全部 `TryGetValue` | `Clients/DashScope/DashScopeRealtimeClient.cs` |

### 第 2 轮（2026-08-04）— 渠道与工具修复

| 编号 | 修复内容 | 涉及文件 |
|:----:|----------|----------|
| A-01/A-07 | `QQChannel` 每次 `new HttpClient()` → 按 botToken 缓存；删除死代码 `_tokenCache` | `Channels/QQChannel.cs` |
| A-02 | `DiscordChannel` 静态 HttpClient → 按 botToken 实例缓存 | `Channels/DiscordChannel.cs` |
| A-08 | `WebhookChannel.ValidateConfigAsync` 恒 true → 校验合法 http/https URL + 更新测试 | `Channels/WebhookChannel.cs` + `ChannelTests.cs` |
| A-09 | 微信渠道字符串搜索解析 JSON → `ToJsonEntity` 结构化解析 | `WeChatMpChannel.cs`、`WeChatKfChannel.cs` |
| A-37 | `McpToolManager.ToSnakeCase` 缩写处理错误 → 修正缩写边界（`GetAPIKey`→`get_api_key`） | `ModelContextProtocol/McpToolManager.cs` |
| A-10 | `ToolSchemaBuilder` 递归无深度限制 → `MaxDepth=8` | `Tools/ToolSchemaBuilder.cs` |
| A-56 | `ChatMessage.ParseMultimodalContent` 吞异常 → XTrace 记录 | `Models/ChatMessage.cs` |
| A-69 | `FilteredChatClient` 空吞后处理异常 → XTrace 记录 | `Clients/FilteredChatClient.cs` |
| A-57 | `AddToolCallDelta` Index 硬编码 0 → `msgs.Count` | `Models/ChatResponse.cs` |
| A-64 | `ChatCompletionRequest.ToChatRequest` 丢 Name/ToolCallId/ReasoningContent → 补齐 | `Clients/OpenAI/ChatCompletionRequest.cs` |
| A-46/A-47 | `BackgroundGenerationService` 清理竞态 + CTS 泄漏 → ReferenceEquals 校验 + 注册前取消旧 CTS | `Services/BackgroundGenerationService.cs` |
| A-48 | `HashTextEmbedder` 注释 v1→v2 | `Embedding/HashTextEmbedder.cs` |
| A-49 | `IAppKey`/`IUserMemory` ExpireTime 注释契约矛盾 → 修正为 MinValue 语义 | `Interfaces/IAppKey.cs`、`IUserMemory.cs` |
| A-50 | `EmbeddingClientMetadata.ProviderName` init+null! → 构造器注入强约束 | `Embedding/IEmbeddingClient.cs` + `OpenAIChatClient.Capabilities.cs` |
| A-51 | `MessageRateLimiter` 注释与实现不符 → 修正注释 | `Services/MessageRateLimiter.cs` |
| A-16/A-17 | `ParallelGroupChat` 共享历史竞态 + 裸 catch → 独立副本 + XTrace 记录 | `Agents/ParallelGroupChat.cs` |
| A-15 | `ConversableAgent.MaxAutoReply` 死属性 → 删除 + 更新测试 | `Agents/ConversableAgent.cs` + `AgentSystemTests.cs` |
| A-14/A-26 | `FunctionCallingPlanner` 重规划预算浪费 + 死代码 → 二次遍历接入重规划循环 + 删死代码 | `Agents/FunctionCallingPlanner.cs` |
| A-18~A-22 | `CodingTools` 池化 StringBuilder 未归还 / cmd.exe 跨平台 / 超时魔法数字 / 搜索无权限目录 / 全量读文件 → 全部修复 | `Coding/CodingTools.cs` |
| A-23 | ~~`CodingAgent._currentPhase` 竞态 → `_phaseLock`~~ → **误报**：`String` 引用原子性无需锁，已回滚 `_phaseLock` | `Coding/CodingAgent.cs` |
| A-25 | `CopilotSkillLoader.MatchGlob` 相对 glob 匹配绝对路径失败 → 支持 `**/` 前缀 | `Coding/CopilotSkillLoader.cs` |

### 第 3 轮（2026-08-04）— Clients/MCP 修复

| 编号 | 修复内容 | 涉及文件 |
|:----:|----------|----------|
| A-59 | `AiClientBase.Dispose` 空实现 → `_ownsHttpClient` 区分释放内部创建的 HttpClient | `Clients/AiClientBase.cs` + `OllamaIntegrationTests.cs`（修复 using 作用域） |
| A-60/A-65/A-66 | `AiClientRegistry` 无锁并发 → `ConcurrentDictionary` + 旧 DisplayName 清理 + GetTypes 异常降级 | `Clients/AiClientRegistry.cs` |
| A-61/A-62 | `DashScopeRealtimeClient` _cts 死状态 + 连接失败泄漏 → 链接 token + try/catch 释放 | `Clients/DashScope/DashScopeRealtimeClient.cs` |
| A-63 | `ChatClientBuilder.SetInnerClient` 静默覆盖 → 释放旧客户端 | `Clients/ChatClientBuilder.cs` |
| A-67 | `AiClientBase` 改写调用方 request.Stream/Model → finally 恢复 | `Clients/AiClientBase.cs` |
| A-72 | `AiClientBase._host` 非 readonly → readonly | `Clients/AiClientBase.cs` |
| A-38/A-39 | MCP 死代码清理：删除 `ToolInfo` 空类、`ProgressNotification`/`ProgressParams` | `ModelContextProtocol/` |
| A-40 | `MapMcp<TTools>` 未注册工具 → 创建 server 后调用 `AddTool<TTools>` | `NewLife.AI.Extensions/McpExtensions.cs` |
| A-33 | MCP IProgress 支持名存实亡 → 评估为框架限制（工具不依赖进度），降级记录 | — |

### 第 4 轮（2026-08-04）— 收尾修复

| 编号 | 修复内容 | 涉及文件 |
|:----:|----------|----------|
| A-32 | `ChatHandlerChain` 懒缓存 `??=` 无锁 → `GetHandlers` 锁内双检 | `Handlers/ChatHandlerChain.cs` |
| A-35 | `HttpMcpServer` Connection! 判空 + A-36 请求体大小限制（1MB DoS 防护） | `ModelContextProtocol/HttpMcpServer.cs` |
| A-11 | `ToolHelper.IsSsrfRisk` 增加 DNS 解析（域名指向内网拦截）+ IPv6 私网段 + 进程级缓存 | `Tools/ToolHelper.cs` + `NativeToolTests.cs` |
| A-45 | `FunctionCallingPlan` 完成后填充 `FinalAnswer`，兑现接口承诺 | `Planner/FunctionCallingPlanner.cs` |
| A-68 | `CombineApiUrl` 无前导斜杠 path 规范化 | `Clients/AiClientBase.cs` |
| A-70 | 确认 MaxCompletionTokens 三路径已覆盖（R4-2），误报关闭 | — |

**验证**：`NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1484 通过 / 0 失败 / 7 跳过**（基线 1466，新增 18 个测试）。

---

## 6. 残留风险

| 风险 | 说明 | 缓解 |
|------|------|------|
| 渠道真实网络发送 | 11 渠道多为真实 HTTP 调用，无法单测覆盖发送成功路径 | 纯逻辑（配置校验/Markdown 转换）单测 + 错误路径 mock |
| A-31 MCP 同步阻塞 | `McpServer.Process` 同步公共 API，工具为 async Task 时内部同步等待 | 内存 body 读取不阻塞线程；保持同步签名；如需异步化需 API 变更 |
| A-71 实时多事件拼接 | 单条 WS 消息含多个 JSON 事件时解析失败被静默丢弃 | DashScope 实时协议通常一条消息一个事件；精细解析留待后续 |
| DNS 解析缓存 | `ToolHelper` DNS 缓存进程级，主机名 IP 变化需重启才刷新 | 默认 5 分钟内的短会话场景可接受；长时间运行可增加 TTL 失效 |

---

## 7. 分析结论（收敛）

经过 **4 轮深入审计**（2026-08-04），`NewLife.AI` 基础库 12 维度全部扫描完成，共登记 **72 个问题**（2 P0、6 P1、36 P2、28 P3），其中 **68 个已修复**、**3 个暂缓**、**1 个误报关闭**：

| 维度 | 问题数 | 已修 | 暂缓/关闭 |
|------|:------:|:----:|:--------:|
| D1 协议客户端残留 | 15 | 13 | A-03（安全做法）、A-31/71（见上） |
| D2 工具调用引擎 | 2 | 2 | — |
| D3 IM 渠道 | 9 | 9 | — |
| D4 智能体框架 | 8 | 8 | — |
| D5 编程智能体 | 7 | 7 | — |
| D6 嵌入 | 2 | 2 | — |
| D7 过滤器 | 0 | — | — |
| D8 Handler 链 | 1 | 1 | — |
| D9 业务接口契约 | 1 | 1 | — |
| D10 记忆存储 | 0 | — | — |
| D11 MCP | 14 | 13 | A-41（扩展点保留） |
| D12 模型 DTO/服务/规划器 | 13 | 13 | — |

**验证基线**：`NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1495 通过 / 0 失败 / 7 跳过**。

**遗留说明**：暂缓项均为低风险或需公共 API 变更的权衡决策；核心链路（协议客户端、工具循环、渠道、MCP、Planner）健壮性已全面加固。NewLife.AI 库自身编译警告为 **0**。

---

（完）
