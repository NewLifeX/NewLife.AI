# NewLife.AI 协议客户端分析

> 版本：v1.0 | 日期：2026-08-03
> 范围：`NAI/NewLife.AI/Clients/` 协议客户端部分的 **chat / stream / think** 基础对话与增强对话能力。工具（Tool）相关由独立分析覆盖，不在此文档展开。

---

## 1. 概述

本文档对 `NewLife.AI` 基础库的协议客户端部分做深入分析，目标是**确保所有支持的协议类型和模型服务商在 chat / stream / think 上稳定可靠使用**。

分析覆盖：
- 6 类协议实现（OpenAI Chat Completions、Anthropic Messages、Google Gemini、DashScope 原生、Ollama 原生、AWS Bedrock Converse）
- 37+ 个 OpenAI 兼容服务商（`BuiltinChatClient` 注册）
- 专用客户端（DeepSeek、AzureAI、NewLifeAI 多协议网关）

---

## 2. 协议客户端总览

### 2.1 架构

主体系为 **`AiClientBase` / `IChatClient` + `AiClientRegistry`**：

```text
AiClientRegistry（[AiClient] / [AiClientModel] 特性扫描注册）
  └─ AiClientDescriptor（无状态描述符 + Factory）
       └─ IChatClient 实例（AiClientBase 子类，绑定 Endpoint/ApiKey）
            ├─ GetResponseAsync          → 非流式
            └─ GetStreamingResponseAsync → 流式
                 └─ ChatStreamAsync（协议级 SSE/NDJSON 解析）
```

统一契约：
- `IChatRequest` / `IChatResponse`：跨协议统一请求/响应接口
- `ChatRequest` / `ChatResponse`：内部统一传输模型
- 各协议请求 DTO（`ChatCompletionRequest`/`AnthropicRequest`/`GeminiRequest`/`DashScopeRequest`/`OllamaChatRequest`/`BedrockRequest`）实现 `IChatRequest` 并实现 `FromChatRequest`/`ToChatRequest` 双向转换
- 各协议响应 DTO 实现 `IChatResponse`，统一暴露 `Messages`/`Usage`/`Text`

### 2.2 协议客户端清单

| 协议 | 客户端 | 基类 | 认证 | 端点形态 |
|------|--------|------|------|----------|
| OpenAI Chat Completions | `OpenAIClientBase` / `OpenAIChatClient` | `AiClientBase` | Bearer | `{endpoint}/v1/chat/completions` |
| OpenAI 兼容服务商（37+） | `BuiltinChatClient`（`partial OpenAIChatClient`） | `OpenAIChatClient` | Bearer | 各自 `ChatPath` 覆盖 |
| Anthropic Messages | `AnthropicChatClient` | `AiClientBase` | `x-api-key` + `anthropic-version` | `{endpoint}/v1/messages` |
| Google Gemini | `GeminiChatClient` | `AiClientBase` | URL `key` 参数 | `{endpoint}/v1/models/{model}:generateContent` |
| DashScope 原生 | `DashScopeChatClient`（原生模式） | `OpenAIChatClient` | Bearer | `{host}/api/v1/services/aigc/...` |
| DashScope 兼容 | `DashScopeChatClient`（兼容模式） | `OpenAIChatClient` | Bearer | `{host}/compatible-mode/v1/chat/completions` |
| Ollama 原生 | `OllamaChatClient` | `AiClientBase` | 可选 Bearer | `{endpoint}/api/chat`（NDJSON） |
| AWS Bedrock Converse | `BedrockChatClient` | `AiClientBase` | AWS SigV4 | `{region}.amazonaws.com/model/{id}/converse` |
| 深度求索 | `DeepSeekChatClient` | `OpenAIClientBase` | Bearer | `https://api.deepseek.com` |
| Azure OpenAI | `AzureAIChatClient` | `OpenAIClientBase` | `api-key` | `{endpoint}/openai/deployments/{model}/chat/completions` |
| 新生命 AI 网关 | `NewLifeAIChatClient` | `OpenAIChatClient` | Bearer | `https://ai.newlifex.com`（多协议端点） |

### 2.3 遗留体系清理（Phase 0）

原 `Providers/` 目录下 7 个旧 Provider 类（`IAiProvider`/`OpenAiProvider`/`AnthropicProvider`/`DashScopeProvider`/`GeminiProvider`/`NewLifeAiProvider`/`OllamaProvider`）为旧协议实现，已通过 `<Compile Remove>` 排除编译，运行时已全部改用 `AiClientRegistry`（`ModelService`/`GatewayService`/`模型配置.Biz`/`ModelPricingService` 均走 `GetDescriptor`）。经全工作区引用检查确认无任何代码依赖后，已于 2026-08-03 删除，并同步清理 `NewLife.AI.csproj` 的 `<Compile Remove>` 条目与 `IChatClient.AsBuilder` 的过时示例注释。

---

## 3. 协议矩阵（chat / stream / think）

| 协议 | 非流式 chat | 流式 stream | 思考 think 请求映射 | 思考 think 响应提取 | 用量统计 |
|------|:---:|:---:|------|------|------|
| OpenAI Chat Completions | ✅ | ✅ SSE | ⚠️ `enable_thinking`（Qwen 参数）+ `reasoning_effort` | ✅ `reasoning_content`/`reasoning` | ✅ 末 chunk 全量 |
| Anthropic Messages | ✅ | ✅ SSE（`event:`/`data:`） | ❌ 未映射（Phase 2 修复） | ✅ `thinking` block / `thinking_delta` | ⚠️ 流式 input 丢失（Phase 3 修复） |
| Google Gemini | ✅ | ✅ SSE | ❌ 未映射（Phase 2 修复） | ✅ `thought=true` part | ✅ 每 chunk 累积 |
| DashScope 原生 | ✅ | ✅ SSE（`event:`/`data:`） | ✅ `enable_thinking` + `thinking_budget` | ✅ `reasoning_content` | ✅ |
| DashScope 兼容 | ✅ | ✅ SSE | ✅ `enable_thinking` | ✅ `reasoning_content` | ✅ |
| Ollama 原生 | ✅ | ✅ NDJSON | ✅ `think` 参数 | ✅ `thinking` 字段 | ✅ |
| AWS Bedrock Converse | ✅ | ✅ SSE（`event:`/`data:`） | ❌ 未映射（Phase 2 修复，尽力） | ✅ `reasoningContent` | ✅ |

---

## 4. chat — 基础对话能力

### 4.1 统一请求构建

各协议通过 `FromChatRequest(IChatRequest)` 将统一请求转换为协议 DTO：
- `ChatCompletionRequest.FromChatRequest` / `BuildBody`：字典方式构建，仅含非空字段（避免 qwen-max 等严格校验拒绝 null），支持多模态 `Contents`（text/image/audio）与工具消息
- `AnthropicRequest.FromChatRequest`：`system` 消息提升为顶级字段、`stop`→`stop_sequences`、工具 `tool_use`/`tool_result` 内容块
- `GeminiRequest.FromChatRequest`：`contents` 数组、角色 `model`（非 assistant）、`systemInstruction` 顶级字段
- `DashScopeRequest.FromChatRequest`：`input.messages` 嵌套、`parameters` 嵌套、多模态内容块
- `OllamaChatRequest.FromChatRequest`：`messages` + `options`
- `BedrockRequest.FromChatRequest`：Converse `messages` + `inferenceConfig`

### 4.2 统一响应解析

各协议响应 DTO 实现 `IChatResponse`：
- `ChatCompletionResponse`：`choices[].message` / `delta`，`usage`（`prompt_tokens`/`completion_tokens`）
- `AnthropicResponse`：`content[]`（text/thinking/tool_use），`usage`
- `GeminiResponse`：`candidates[].content.parts[]`，`usageMetadata`
- `DashScopeResponse`：`output.choices[]`，`usage`
- `OllamaChatResponse`：`message` + `done`
- `BedrockResponse`：`output.message.content[]`，`usage`

### 4.3 健壮性

- ✅ 非法 assistant 消息跳过：`ShouldSkipAssistantMessage`（DeepSeek 等要求 assistant 至少含 content 或 tool_calls）
- ✅ 版本路径去重：`CombineApiUrl` 防止 `{endpoint}/v1` + `/v1/...` 产生双版本路径
- ✅ 错误响应：`PostAsync` 非 2xx 抛 `ApiException`（状态码 + 响应体）
- ✅ 空消息校验：`AiClientBase.GetResponseAsync`/`GetStreamingResponseAsync` 空消息抛 `ArgumentException`

---

## 5. stream — 流式对话能力

### 5.1 各协议流式格式

| 协议 | 流式格式 | SSE 前缀 | 终止标记 |
|------|----------|----------|----------|
| OpenAI 兼容 | SSE | `data: `（带空格） | `data: [DONE]` |
| Anthropic | SSE | `event:` + `data:`（无空格） | `message_stop` |
| Gemini | SSE | `data: `（带空格） | 流结束 |
| DashScope 原生 | SSE | `id:`/`event:`/`data:` | 流结束 |
| DashScope 兼容 | SSE | `data:`（无空格） | `[DONE]` |
| Ollama | NDJSON | 无前缀，逐行 JSON | `"done":true` |
| Bedrock | SSE | `event:` + `data:` | `messageStop` |

### 5.2 已修复缺口（Phase 3，2026-08-03）

| # | 缺口 | 影响 | 修复 | 状态 |
|---|------|------|------|:---:|
| S1 | **Anthropic 流式用量丢失**：`AiClientBase.GetStreamingResponseAsync` 只取最后一个非空 chunk 的 Usage；Anthropic 的 input/output token 分 `message_start`/`message_delta` 两个互补 chunk 返回，流式路径 `InputTokens` 恒为 0。下游 `MessageFlow`（Web）与 `GatewayService`（网关）同样取末 chunk，导致**用量记录/会话 Token 统计/计费错误**。`MergeChunkUsage` 覆写仅在 `ToolChatClient` 路径生效 | 计费与统计错误 | `GetStreamingResponseAsync` 改用 `MergeChunkUsage` 合并并回写 chunk.Usage | ✅ |
| S2 | **SSE 前缀不统一**：OpenAI 兼容要求 `data: `（带空格），DashScope 兼容用 `data:`；`[DONE]` 在 DashScope 兼容路径被静默跳过继续循环 | 部分服务商返回 `data:`（无空格）时整行被丢弃，流式中断 | 统一前缀兼容判断（忽略大小写）+ `[DONE]` 统一 break | ✅ |
| S3 | **流式错误静默吞掉**：OpenAI 兼容 / DashScope 兼容 / NewLifeAI 路径 `try { ParseChunk } catch { }` 静默吞异常；流式 `error` 对象被当作普通 chunk 丢弃 | 协议变更/服务商错误无感知，表现为静默截断 | 新增 `AiClientBase.EnsureNoStreamError` 识别流式 `error` 对象并抛异常；Anthropic/Bedrock 处理 `event:error`；Ollama 处理 NDJSON error | ✅ |

### 5.3 已健壮的部分

- ✅ 取消令牌：各协议流式循环均检查 `cancellationToken.ThrowIfCancellationRequested()`
- ✅ Anthropic 流式事件模型完整：`message_start`/`content_block_start`/`content_block_delta`（text/thinking）/`content_block_stop`/`message_delta`/`message_stop`
- ✅ Ollama 流式 tool_calls 语义：`done=false` 中间帧出现 tool_calls 时，终结帧覆写 `FinishReason=ToolCalls`，保证工具循环正确进入
- ✅ DashScope 原生流式错误：`event:error` 时解析 code/message 并抛 `HttpRequestException`

---

## 6. think — 思考模式能力

### 6.1 统一选项

- `ChatOptions.EnableThinking`（`Boolean?`）：null=不设置、true=开启、false=关闭
- `ChatOptions.ReasoningEffort`（`String?`）：推理强度（OpenAI o 系列 low/medium/high；DeepSeek high/max）
- 协议专属：`Items["ThinkingBudget"]`（DashScope 原生 `thinking_budget`）

### 6.2 各协议映射现状

| 协议 | EnableThinking → 协议字段 | ReasoningEffort | 状态 |
|------|---------------------------|-----------------|------|
| OpenAI 兼容 | `enable_thinking`（DashScope/Qwen 参数） | `reasoning_effort` | ⚠️ 兼容模式可用；OpenAI 原生不认识 `enable_thinking`（o 系列应走 `reasoning_effort`，忽略即可） |
| DeepSeek | `thinking: {type: "enabled"/"disabled"}` | `reasoning_effort` | ✅ |
| DashScope 原生 | `enable_thinking` + `thinking_budget` | — | ✅ |
| Ollama | `think` | — | ✅ |
| **Anthropic** | **未映射（`[IgnoreDataMember]`，静默丢弃）** | **未映射** | ❌ Phase 2 修复 |
| **Gemini** | **未映射** | **未映射** | ❌ Phase 2 修复 |
| **Bedrock** | **未映射** | **未映射** | ❌ Phase 2 修复（尽力） |

### 6.3 响应侧提取（已就绪）

| 协议 | 思考响应字段 | 提取位置 |
|------|-------------|----------|
| OpenAI 兼容 | `reasoning_content` / `reasoning` | `OpenAIClientBase.ParseChatMessage` → `ChatMessage.ReasoningContent` |
| Anthropic | `content[].type=thinking` / `thinking_delta` | `AnthropicResponse.ToChatResponse` / `ToChunkResponse` |
| Gemini | `part.thought=true` | `GeminiResponse` parts 解析 |
| DashScope | `reasoning_content` | 同上（OpenAI 兼容 + 原生适配） |
| Ollama | `message.thinking` | `OllamaChatResponse` → `ReasoningContent` |
| Bedrock | `content[].reasoningContent.reasoningText` | `BedrockResponse` |

### 6.4 已修复缺口（Phase 2，2026-08-03）

| # | 缺口 | 影响 | 修复 | 状态 |
|---|------|------|------|:---:|
| T1 | **Anthropic `EnableThinking`/`ReasoningEffort` 静默丢弃**：未映射到 `thinking: {type:"enabled", budget_tokens:N}`，Claude extended thinking 无法通过统一 API 控制 | 思考模式在 Anthropic 协议下不可用 | `AnthropicRequest` 新增 `Thinking` 字段，`EnableThinking` → `thinking:{type,budget_tokens}`（默认预算 1024，`ThinkingBudget` 可覆盖，max_tokens 自动兜底提升） | ✅ |
| T2 | **Gemini `EnableThinking`/`ReasoningEffort` 静默丢弃**：未映射到 `generationConfig.thinkingConfig.thinkingBudget`，Gemini 2.5 思考预算不可控 | 思考预算在 Gemini 协议下不可控 | `GeminiGenerationConfig` 新增 `ThinkingConfig`，`EnableThinking` → `thinkingConfig.thinkingBudget`（true=默认 1024，false=0 关闭） | ✅ |
| T3 | **Bedrock `EnableThinking` 静默丢弃**：Converse thinking 未映射（模型相关） | Claude-on-Bedrock 思考不可控 | `BedrockRequest` 新增 `AdditionalModelRequestFields`（官方支持字段），`EnableThinking` → 透传 Anthropic 格式 thinking（含 budget_tokens） | ✅ |

---

## 7. 测试覆盖现状

### 7.1 已有测试

- **DTO 序列化单元测试**（较全）：`ChatCompletionRequestTests`、`ChatCompletionResponseTests`、`AnthropicRequestTests`/`AnthropicResponseTests`、`GeminiRequestTests`/`GeminiResponseTests`、`BedrockChatClientTests`、`OllamaChatModelTests`、`AiClientBaseTests`（`CombineApiUrl`）
- **真实 Key 集成测试**（`RequiresApiKeyFact`，无 Key 跳过）：`DashScopeIntegrationTests`、`DeepSeekIntegrationTests`、`OpenAiChatClientIntegrationTests`（走 DashScope 兼容端点）、`OllamaIntegrationTests`、`NewLifeAiIntegrationTests`、`DashScopeOmniIntegrationTests`、`DashScopeTtsTests` 等
- **注册表测试**：`AiProviderTests`（46 个内置服务商注册）、`DashScopeAdvancedTests`

### 7.2 已补齐覆盖空白（Phase 4，2026-08-03）

| # | 空白 | 覆盖 | 状态 |
|---|------|------|:---:|
| C1 | **无 stub HttpClient / mock SSE 流协议级测试**：流式解析循环（SSE/NDJSON）无自动化验证 | 新增 `StubHttpMessageHandler`（可注入 `AiClientBase.HttpClient`）+ 六协议测试类（`*ChatClientStreamTests`），覆盖非流式/流式/思考/错误路径，无需真实 Key | ✅ |
| C2 | **think 映射无测试**：各协议 `EnableThinking` → 协议字段的序列化断言缺失 | 各测试类断言请求体序列化（`enable_thinking`/`thinking`/`thinkingConfig`/`think`/`additionalModelRequestFields`）；`AnthropicRequestTests` 更正错误断言并补 4 个 think 用例 | ✅ |
| C3 | **流式用量合并无验证**：Anthropic 分块 Usage 合并无验证 | `AnthropicChatClientStreamTests` 验证 message_start + message_delta → 合并后 input/output 完整 | ✅ |

---

## 8. 缺口清单与优先级

全部缺口已在本轮修复并补测试（2026-08-03）。

| 优先级 | 编码 | 缺口 | 模块 | 修复 | 状态 |
|:---:|------|------|------|------|:---:|
| P0 | S1 | Anthropic 流式用量丢失（计费错误） | stream | Phase 3 | ✅ |
| P0 | T1/T2 | Anthropic/Gemini think 参数静默丢弃 | think | Phase 2 | ✅ |
| P1 | S2 | SSE 前缀与 `[DONE]` 处理不统一 | stream | Phase 3 | ✅ |
| P1 | S3 | 流式错误静默吞掉 | stream | Phase 3 | ✅ |
| P1 | T3 | Bedrock think 参数未映射 | think | Phase 2 | ✅ |
| P1 | C1/C2/C3 | mock 协议测试 + think/usage 测试缺失 | 测试 | Phase 4 | ✅ |

---

## 9. 修复记录

### Phase 0（2026-08-03）— 清理旧 Providers 死代码
- 删除 `NAI/NewLife.AI/Providers/` 全部 7 个旧 Provider 文件（已排除编译、无代码引用）
- 清理 `NewLife.AI.csproj` 的 `<Compile Remove>` 条目
- 修正 `IChatClient.AsBuilder` 过时示例注释（`DashScopeProvider` → `AiClientRegistry`）

### Phase 1（2026-08-03）— 分析报告
- 产出本文档：协议矩阵、chat/stream/think 现状与缺口表、测试覆盖、残留风险

### Phase 2（2026-08-03）— think 缺口修复
- `AnthropicRequest`：新增 `Thinking`/`AnthropicThinkingConfig`，`EnableThinking` → `thinking:{type,budget_tokens}`（默认预算 1024，`ThinkingBudget` 覆盖，max_tokens 自动兜底）
- `GeminiRequest`：`GeminiGenerationConfig` 新增 `ThinkingConfig`/`GeminiThinkingConfig`，`EnableThinking` → `thinkingConfig.thinkingBudget`
- `BedrockRequest`：新增 `AdditionalModelRequestFields`，`EnableThinking` → 透传 Anthropic 格式 thinking（官方支持的 `additionalModelRequestFields` 字段）
- 更正 `AnthropicRequestTests.FromChatRequest_Thinking` 错误断言，补 4 个 think 映射用例

### Phase 3（2026-08-03）— stream 健壮性修复
- S1：`AiClientBase.GetStreamingResponseAsync` 用 `MergeChunkUsage` 合并并回写 chunk.Usage，Anthropic 流式用量完整（下游 MessageFlow/GatewayService 一并受益）
- S2：SSE 前缀统一为 `data:`（忽略大小写）+ `[DONE]` 统一 break（OpenAI 兼容 / DashScope 兼容 ×2 / NewLifeAI ×2）
- S3：新增 `AiClientBase.EnsureNoStreamError`，OpenAI 兼容路径识别流式 error 对象并抛异常；Anthropic/Bedrock 处理 `event:error`；Ollama 处理 NDJSON error

### Phase 4（2026-08-03）— mock 测试基建 + 协议级测试
- 新增 `XUnitTest/Helpers/StubHttpMessageHandler.cs`（可注入 `AiClientBase.HttpClient`，支持 JSON/SSE/NDJSON 预制响应 + 请求头捕获）
- 新增 6 个协议级测试类（42 用例）：OpenAI（SSE/`[DONE]`/reasoning/error）、Anthropic（event/data/thinking_delta/usage 合并/error）、Gemini（thought=true/thinkingConfig）、DashScope 原生（SSE/error）、Ollama（NDJSON/think/error）、Bedrock（SigV4/reasoning/嵌套 SSE/error）
- 修复 `GeminiResponse.Text` 跳过 thought part（正文不应含思考内容）

### Phase 5（2026-08-03）— 验证与回归
- `NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1355 通过 / 0 失败 / 7 跳过**（跳过为未配置 Key 的集成测试）
- **S3 修复暴露既有测试假通过**：`DashScopeOmniIntegrationTests.OmniStream_AudioModality` 使用不支持的音色 `Cherry`（服务商错误被静默吞掉一直假通过），`EnsureNoStreamError` 正确抛出后，按官方文档改为默认音色 `Tina` 后真实通过
- `星语.sln` 全量构建因运行中的 StarChat 进程（PID 11300）锁定 `NewLife.AI.dll` 未执行；改动全部在 `NAI/` 内，NAI 解决方案已覆盖验证

---

## 10. 残留风险

| 风险 | 说明 | 缓解 |
|------|------|------|
| Anthropic/Gemini/Bedrock 无真实 Key | 无法做真实网络验证 | mock 协议测试 + 请求序列化断言保障；集成测试待 Key 后可运行（如 DashScope/DeepSeek/Ollama 已真实回归） |
| OpenAI 原生 `enable_thinking` | OpenAI 官方不认识此参数（o 系列用 `reasoning_effort`），但不影响兼容服务商 | 保留（DashScope/Qwen 依赖），文档澄清；o 系列走 `reasoning_effort` |
| Bedrock think 映射依赖模型 | Converse thinking 行为随底座模型（Claude/Llama/Mistral/Nova）差异 | 已按官方 `additionalModelRequestFields` 文档实现并验证序列化；行为以官方文档为准 |
| 流式错误现在会显式抛出 | S3 修复后，服务商流式错误不再静默吞掉，可能暴露此前被掩盖的问题（如测试用错音色） | 属预期行为；消费方需对 `HttpRequestException` 做错误处理 |

---

（完）
